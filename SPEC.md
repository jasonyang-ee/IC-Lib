<!-- SPEC FORMAT (baked by /encode-docs — keep; makes this file self-describing)
Sections, fixed order: §G goal | §C constraints | §I interfaces | §R research? | §V invariants
Symbols: → leads to | ∴ therefore | ∀ every | ∃ some | ! must | ? may/unknown | ⊥ never | ≠ | ∈ | ∉ | ≤ | ≥ | & and | § section
Durable truth only. Mutable: add sparingly (high bar), prune freely on evidence.
Address §<S>.<n> — §V.2 = invariants item 2. Commits/PRs cite by §.
Encoding: drop articles/filler/aux verbs. Fragments fine. Short synonyms (fix > implement).
Preserve verbatim: code, paths, identifiers, URLs, numbers, error strings, SQL, regex.
Tables (§C/§I/§R/§V): pipe-delimited, id-keyed; header row + GFM delimiter row (|---|---|), one cell per column. Escape literal \| . Empty cell = -
ids: monotonic, never reused — take the next from `next:` below, ⊥ from the highest row (rows get pruned)
next: C11 I12 R1 V54
One file rule: >1000 lines → prune stale §V, ⊥ split into more files.
Full rules: /encode-docs skill. Cutting a word that loses a fact ⊥ allowed.
-->

# IC-Lib SPEC

## §G GOAL

G1: manage PCB/OrCAD component lib end-to-end: vendor intake -> CAD asset control -> approval/status control -> inventory/project/BOM reuse.
G2: keep app operable on fresh or drifted DB via startup init, numeric migrations, auth bootstrap, file scan/repair, email/runtime config.
G3: provide audit/reporting/admin surfaces for governed library ops.

## §C CONSTRAINTS

id|description
|---|---|
C1|stack React 19 + Vite + TailwindCSS v4 + React Query 5 \| Express 4 \| PostgreSQL 18 \| JWT cookie auth.
C2|DB primary keys ! `UUID DEFAULT uuidv7()`; create time derive via `created_at(id)`.
C3|app owns flat CAD tree `library/footprint\|symbol\|model\|pspice\|pad` + temp/delete buffers; DB tracks `cad_files` + `component_cad_files`, plus `footprint_related_cad_files` for reusable footprint-driven pad and 3D-model history.
C4|views `components_full`, `component_specifications_view`, `eco_orders_full`, `production_parts`, `prototype_parts`, `archived_parts`, `alternative_parts` = external OrCAD-CIS/ODBC compat surface only — server runtime ⊥ query them; keep + startup-verify all 7 (`EXPECTED_SCHEMA_VIEWS`, locked by `schemaInspectionService.test.js`); OrCAD/CIS compat ! keep TEXT cols `pcb_footprint\|schematic\|step_model\|pspice\|pad_file`. note: live `schema_migrations` holds historical row `0_schema_version_1_8_0.sql` (file ∉ repo) — inert, pending-detection = files minus rows; ⊥ delete history. note: `footprint_sources` table legacy — 0 rows live, ∄ writer (footprint fetch stages via `library/temp` instead); kept in schema/backup/clear lists, ⊥ new writes.
C5|roles `read-only\|reviewer\|lab\|read-write\|approver\|admin`; `lab` = `read-write` except File Library page/browse/manage access ⊥; some component-scoped CAD helper APIs still usable from Parts Library edit flow.
C6|runtime flags/env ! support `CONFIG_ECO`, `CONFIG_BASE_URL`, `CONFIG_SUBDIRECTORY_PATH`, DB creds, vendor API creds, `SMTP_ENCRYPTION_KEY`.
C7|startup may repair missing base schema from `database/init-*.sql`; incremental change ! live in `database/migrations/<int>_<desc>.sql`.
C8|file ops & import/repair flows must work on shared repo filesystem; path escape ⊥.
C9|spec target code-as-built first; known drift/bugs tracked via `CHANGELOG.md` + git history, ⊥ hidden.
C10|flexible workflow/audit payloads may persist in JSONB: `activity_log.details`, distributor `price_breaks`, ECO alt `distributors`, similar variable-shape data.

## §I INTERFACES

id|type|shape → output
|---|---|---|
I1|ui|`/login\|/\|/library\|/file-library\|/inventory\|/vendor-search\|/projects\|/reports\|/audit\|/user-settings\|/admin-settings\|/eco?` -> React SPA; nav gated by role + `ecoEnabled`.
I2|api|`/api/auth/*` -> login/logout/verify, password change, profile, file-storage-path, ECO prefs, admin user CRUD; `/api/auth/oidc/status\|login\|callback` -> SSO flow (V29).
I3|api|`/api/components/*\|/api/categories/*\|/api/manufacturers/*\|/api/distributors/*` -> catalog read, component CRUD, spec/distributor/alt-part CRUD, approval, category change, bulk vendor refresh, component project-assignment read for library view.
I4|api|`/api/search/*` -> Digikey/Mouser/all-vendor lookup, add-to-library draft seed, Ultra Librarian/SnapEDA footprint fetch.
I5|api|`/api/inventory/*` -> inventory read, low-stock, barcode search, alt inventory read/update, inventory CRUD.
I6|api|`/api/files/*\|/api/file-library/*` -> temp upload/finalize/cleanup/export, collision check, CAD browse/search/orphans/link/unlink/rename/delete/scan.
I7|api|`/api/eco/*` -> ECO list/detail/create/delete/approve/reject/pdf/last-rejected + stage CRUD/import/export/approver assignment.
I8|api|`/api/projects/*` -> project CRUD, project component add/update/remove, consume inventory; project detail rows include lowest-break unit pricing when distributor data exists; client BOM export via `client/src/utils/bomExport.js`.
I9|api|`/api/dashboard/*\|/api/reports/*\|/api/settings/*\|/api/smtp/*\|/api/admin/*` -> stats, audit feed/clear, reports, feature flags, category/BOM/ECO/email/db ops, CIS/label downloads, SMTP test, admin init/reset/verify.
I10|cmd|`./start.sh` -> dev server; `./test.sh` -> lint + tests; `node scripts/import.js [--dry-run\|--file=<category>]` -> legacy CSV import; `cd server && npm run repair -- admin-reset` -> reset default `admin` password.
I11|env|`JWT_SECRET` ! set; `CONFIG_ECO`, `CONFIG_BASE_URL`, `CONFIG_SUBDIRECTORY_PATH`, `DB_*`, vendor API creds, `SMTP_ENCRYPTION_KEY`; optional `OIDC_ISSUER_URL\|OIDC_CLIENT_ID\|OIDC_CLIENT_SECRET\|OIDC_REDIRECT_URI\|OIDC_SCOPES\|OIDC_PROVIDER_NAME\|OIDC_DEFAULT_ROLE` (SSO on iff issuer+client id).

## §V INVARIANTS

id|invariant definition
|---|---|
V1|`JWT_SECRET` ! set; auth token via HttpOnly cookie `AUTH_COOKIE_NAME` 24h or Bearer fallback; missing\|invalid\|expired -> 401.
V2|role gates hold: `read-only` ⊥ write; `reviewer` ⊥ write & ∈ ECO approval; `lab\|read-write\|approver\|admin` ∈ lib/project/inventory mutate; `lab` ⊥ File Library page/browse/manage routes but may use component-scoped CAD helper APIs during Parts Library edit; `approver\|admin` ∈ hard CAD delete; `admin` ∈ admin/user/settings mutate.
V3|default client route: limited role -> `/eco` if eco flag on else `/library`; full role -> `/`.
V4|startup detects blank DB = ∄ all of `component_categories\|manufacturers\|distributors\|components`. blank -> run `database/init-schema.sql` then migrations; existing (≥1 critical table) -> migrations only, ⊥ re-init (partial DB still missing objects -> boot fail @ schema inspection, ⊥ silent half-init). `database/init-users.sql` runs only when `users` table ∄. `database/init-settings.sql` idempotent-seeds every boot (ON CONFLICT DO NOTHING), ≠ init. migrations apply numeric order w/ full server log: found/applied/pending counts + per-file apply/complete + executed summary. post-migration missing required table/view/col -> boot fail. ⊥ inline `ALTER` in app code; cad_files.missing owned by init-schema + migration 1.
V5|migration filename contract: new files `database/migrations/<int>_<desc>.sql`; sort by int, not lexicographic; legacy zero-pad still parse numeric.
V6|component status ∈ `{new,reviewing,prototype,production,archived}`; ECO status ∈ `{pending,in_review,approved,rejected}`; create time read from `created_at(uuid)`.
V7|component create ! inventory row ∃, `activity_log` row ∃, joined API payload include category/manufacturer/part_type/created_at.
V8|CAD source-of-truth -> `cad_files` + `component_cad_files`; TEXT CAD cols derived by regen, strip ext, skip `.dra`; startup scan registers untracked files & toggles `cad_files.missing` from disk state; footprint-driven reuse history persists in `footprint_related_cad_files`, auto-links known pad and 3D model files when a footprint is linked, and learns new footprint/pad/model co-usage from component saves, ECO applies, and direct file linking; PSpice uses the existing `pspice` CAD category for both `.lib` libraries and PSpice `.olb` symbols stored under `library/pspice`, while ambiguous staged `.olb` uploads default to schematic first and may be reassigned to PSpice before finalization.
V9|fs ops ! safe leaf names + resolved path ∈ library base; traversal/nested path reject.
V10|public-read boundary is an explicit allowlist, ⊥ blanket: unauthenticated GET only for catalog (`components\|categories\|manufacturers\|distributors` reads), inventory reads (+ `POST /api/inventory/search/barcode` mutation-shaped read), project reads, `reports/*`, dashboard `stats\|recent-activities\|category-breakdown\|extended-stats`, settings reads (`/features\|/\|/eco\|/eco/logo\|/eco/preview\|/global-prefix\|/categories(+specs)`, CIS/label downloads), SSO flow `GET /api/auth/oidc/status\|login\|callback` (pre-session by design, V29) — the guest read-only surface. ∀ other GET ! `authenticate` (+role gate matching consuming page): audit feed `GET /dashboard/activities/all` -> auth; `GET /dashboard/db-info` -> auth; `GET /settings/database/status\|verify` -> auth+admin; eco/fileLibrary/fileUpload/auth/admin/smtp readers stay guarded. allowlist enforced 2-way (unlisted public GET & stale allowlist entry both fail) by `routeAuthGuards.test.js`; adding a public GET ! update test allowlist & this invariant. optional-actor read flows use `req.user?.id \|\| null`.
V11|ECO routes auth ! always; any auth user may view; create -> `canWrite`; approve/reject -> `canApprove`; current-stage actability computed per user + stage.
V12|ECO pipeline tags ∈ `{proto_status_change,prod_status_change,spec,filename,shared_file_rename,distributor,alt_parts}`; legacy `general\|spec_cad\|status_change` normalize; stage match needs status-tag match & detail-tag match.
V13|ECO vote uniqueness ! one effective vote per `(eco_id, stage_id, COALESCE(acting_for_user_id,user_id))`; delegation target ! same\|higher role; delegated user may consume assigned approver slot.
V14|rejected ECO retains lineage via `parent_eco_id`; retry UI may preload rejected field/spec/distributor/alt/CAD deltas + status proposal under same ECO number chain.
V15|Library edit policy: `CONFIG_ECO` on -> admin may direct-edit any part incl direct status reset; non-admin write roles may direct-edit only `approval_status=new` parts while keeping status `new`; controlled parts `reviewing\|prototype\|production\|archived` require ECO for direct field/spec/distributor/alt/category/CAD-link/delete changes. ECO mode blocks live CAD overwrite & stages category/status/field/spec/distributor/alt/CAD changes + notes.
V16|notifications optional: SMTP disabled -> send skip; enabled -> welcome/ECO/doc-control emails use `CONFIG_BASE_URL` fallback chain, user prefs from `email_notification_preferences`, delegation from `users.delegation`, send/fail log in `email_log`.
V17|project/inventory contract: project mutation routes auth+canWrite; inventory mutation routes auth+canWrite; project detail pricing + inventory-value reporting derive from the lowest-quantity distributor price break, using the cheapest matching unit price across distributors; BOM export client-side from project detail + live component/distributor/alternative fetch.
V18|fresh DB repo schema ! cover every persisted API surface present in §I.
V19|fresh DB default ECO stage config ! include every runtime pipeline tag required by §V12.
V20|file-library shared rename policy: actor = `admin` -> direct rename even if file shared; `CONFIG_ECO` on & actor ∉ `admin` & affected parts > 1 & any affected part status != `new` -> shared transparent-gray warning before submit, stage 1 ECO w/ `shared_file_rename` tag, set only affected non-`new` parts `approval_status=reviewing`, approve -> rename shared CAD files, refresh CAD text for staged + skipped `new` parts, restore staged parts original status; reject\|delete -> restore staged parts original status & keep file info unchanged.
V21|ECO status proposal UI/API ! allow `new -> prototype`, `reviewing -> archived`, `prototype -> production\|archived`, `production -> prototype\|archived`, `archived -> prototype\|production`; ECO path back to `new` ⊥; `new`-part ECO without `prototype` proposal ⊥. (truth: `client/src/utils/ecoStatusProposalOptions.js`)
V22|ECO CAD diff/apply key on persisted `file_type`; same-ext `.olb` in `symbol` vs `pspice` ! stay isolated through ECO submit/approve, so link/unlink in one role ⊥ mutate the other role by filename/ext coincidence.
V23|vendor-search append flows ! normalize optional FK lookups before component/alternative distributor writes: blank `manufacturer_id\|distributor_id` -> `null`/skip; alternative create may resolve/create manufacturer by `manufacturer_name` when local ID miss.
V24|admin bulk vendor refresh queues oldest sync first: stock -> supported distributor rows by `distributor_info.last_vendor_sync_at`; specs -> DigiKey-backed components by `components.last_specs_refresh_at`; non-rate-limit completion bumps cursor even when vendor data missing; vendor daily-limit reject -> abort batch @ current item.
V25|CAD fs mutation ! atomic across disk+DB. rename (`renameCadFile`, file-library single-file rename, part-edit `renameFile`, ECO `applyMassFileRenameEco`, `renameFootprintGroup`): physical rename + `cad_files` update + TEXT regen ∈ 1 txn; fail -> DB rollback + best-effort physical revert. delete (`deleteCadFile`): DB row delete + TEXT regen ∈ txn, unlink only post-COMMIT (crash -> harmless on-disk orphan re-surfaced by scan, ⊥ DB row -> missing file). same-inode case-only rename ⊥ collision reject. temp finalize move-then-register self-heals via scan.
V26|component CAD single-file slots ! ≤1 per component: schematic symbol, 3D model, PSpice symbol (`.olb`). PSpice `.lib` libraries multi-allowed. slot keyed by `getCadFileSlot(category,filename)` (role-aware for pspice via `getPspiceFileRole`). upload\|link of 2nd file into an occupied slot -> keep-vs-replace conflict modal (`client/src/components/library/ComponentFiles.jsx`, mirrors symbol/model guard); ECO CAD diff stays `file_type`-isolated per §V22.
V27|∀ state-changing route (POST\|PUT\|PATCH\|DELETE) ! `authenticate` first (per-route or router-level `router.use`); role gate ! match resource write policy (§V2). public exceptions only: `POST /api/auth/login`, `POST /api/inventory/search/barcode` (mutation-shaped read). enforced by full-router sweep `server/src/test/routeAuthGuards.test.js`.
V28|footprint filenames (`.psm`\|`.bsm`\|`.dra`) normalize @ ∀ input boundary (upload, ZIP extract, temp finalize, part-edit rename, File Library single+pair rename, mass-rename ECO staging): whole name lowercase & base dot-free (ext = last-dot segment, every other `.` silently dropped). `+` -> typed `FootprintNameError` -> 422 + client pre-submit toast, ⊥ silent strip. legacy on-disk names grandfathered (scan registers as-is; use_existing links w/o reject). collision checks fire on normalized name -> 409, ⊥ silent overwrite. server truth `server/src/utils/footprintFiles.js`, client mirror `client/src/utils/footprintFiles.js` ! exact parity.
V29|OIDC/SSO federates identity @ login only: callback -> app mints same JWT cookie as local login ∴ `authenticate`/role gates/client AuthContext untouched; ⊥ route trusts IdP tokens as session; ⊥ server-side session store. federated identity key = (`oidc_issuer`,`oidc_sub`) partial-unique on `users`. resolution order: identity match -> user; else verified email (`email_verified===true`) matching exactly 1 non-federated local user -> link; else JIT-provision `auth_provider='oidc'`, `password_hash=NULL`, role=`OIDC_DEFAULT_ROLE` (unset\|invalid -> `read-only`); inactive account -> reject ∀ paths. NULL `password_hash` ! rejected by local login (401) & change-password (400), bcrypt ⊥ called. flow = Auth Code+PKCE via `openid-client` v6: state/nonce/verifier in JWT-signed httpOnly cookie 10m; callback errors -> `/login?error=sso_failed\|account_disabled`, ⊥ bare 500. enabled iff `OIDC_ISSUER_URL`+`OIDC_CLIENT_ID` set; local login stays break-glass. truth `server/src/services/oidcService.js`; locked by `oidcService.test.js`+`oidcController.test.js`.
V30|liveness ≠ readiness. liveness = process up (cheap, ⊥ DB). readiness = can serve = DB reachable + schema verified; DB unreachable -> readiness non-2xx (503), ⊥ 200. orchestrator probe (Dockerfile HEALTHCHECK / compose / k8s) ! consume readiness. `/api/health` ⊥ emit `status:'OK'` when DB down.
V31|process resilience. pool idle-client `error` -> log + evict, ⊥ `process.exit` (recoverable: failover/blip/idle-reset). `uncaughtException`\|`unhandledRejection` -> log FATAL + graceful shutdown + exit≠0. SIGTERM\|SIGINT -> stop accept (`server.close`) + await in-flight ≤ bounded timeout + `pool.end()` + exit 0. termination reserved for unrecoverable-startup \| signal, ⊥ transient backend hiccup.
V32|`POST /api/auth/login` + change-password rate-limited per client -> exceed -> 429; global request ceiling guards §V10 public-read surface from abuse. limiter ⊥ break same-origin nginx-proxied prod (trust proxy / key on X-Forwarded-For correctly).
V33|`project.status ∈ {active,completed,archived}`; reject out-of-domain @ API boundary (400) + DB CHECK constraint (parity w/ `components.check_approval_status`, `eco_orders.check_eco_status`). dashboard active-count ⊥ count undocumented statuses (`planning` phantom removed).
V34|outbound vendor HTTP (DigiKey\|Mouser\|footprint fetch) ! bounded `timeout`; hung upstream -> per-item skip/abort, ⊥ stall bulk batch (§V24) or hold request open ∞.
V35|`/login` = local sign-in + guest read-only sign-in + `Sign in with {provider}` button when OIDC on (`GET /api/auth/oidc/status`); SSO callback error → `/login?error=sso_failed\|account_disabled`; success → requested route if any. post-login default: `read-only\|reviewer` → `/eco` if ECO flag on else `/library`; `lab\|read-write\|approver\|admin` → `/`.
V36|nav by role: `read-only` = Library+ECO; `reviewer` = +User Settings; `lab\|read-write\|approver\|admin` = +Dashboard/Inventory/Vendor Search/Projects/Reports/Audit; `read-write\|approver\|admin` = +File Library; `admin` = +Admin Settings. ECO nav item shown only when ECO flag on. sidebar collapse state + dark mode persist `localStorage`; sidebar shows app version, user card, logout.
V37|UI model: most screens = left filter/nav pane + right detail pane; write flows use modals; toasts carry save/import/update progress. cross-page pivots: Library ↔ Inventory\|Projects\|Vendor Search\|File Library(role-gated), Inventory → Library\|Vendor Search, File Library → Library, Vendor Search → Library, Projects BOM settings → Admin BOM, User file-path → File Library copy-path, Admin scan → File Library new-file visibility.
V38|Dashboard (`/`, full-nav roles only): DB-not-initialized state shows warning + routes admin to settings; shows totals (library/categories/manufacturers/projects/distributors), approval-state counts, stock counts, category distribution, DB info, CAD-health.
V39|Library browse (`/library`): category sidebar, full-text search, sort by PN\|MFG P/N\|value\|desc\|created\|updated, multi-status filter, prev/next step when search parses as PN, virtualized list, copy deep link. view state persists: sort/order/status-filter/category → `localStorage` `viewPrefs:library` (whitelist-validated via `client/src/utils/viewPrefs.js`; corrupt/unknown → defaults; stale category id → reset to All Categories); search term → per-tab `sessionStorage` so add-part round-trips return to the same view.
V40|Library detail: copyable fields, alt selector, datasheet link, approval status/history, assigned-project tile (click → Projects), pivots to Inventory\|Add-to-Project\|File Library(role); CAD links route to File Library deep links only for File-Library roles (`read-write\|approver\|admin`) — `lab` sees plain filenames; distributor panel links to Vendor Search; specs shown read-only.
V41|Library add/edit: manual fields, category numbering/template, manufacturer typeahead+create-new, package/sub-category suggest, spec editor + custom spec + vendor-field mapping, alt editor (promote/delete + distributor rows), CAD manager (schematic\|footprint\|pad\|3D\|PSpice) with file or ZIP upload, staged `.olb` → two-panel assignment popup (defaults schematic, may move to PSpice before save), link-existing-file, footprint link auto-adds known pad+3D from history, rename shortcuts (freeform\|MFG P/N\|package), footprint pairs move together, save-time conflict modal `use_existing\|overwrite`. vendor-assisted entry: Vendor Search opens add mode with prefilled payload, vendor panel `Auto Fill` maps base fields + mapped specs.
V42|Library direct-edit policy mirrors §V15 in the UI: ECO off → write roles edit live + bulk-delete; ECO on → admin edits any part incl. status reset, non-admin write roles edit only `approval_status=new` parts. ECO staging: write roles `Initiate ECO` on `new` parts only when proposing `prototype`; stage field/spec/distributor/alt/CAD/status/delete + notes per §V21; retry reloads rejected change-set under same ECO chain; ECO mode blocks live-file overwrite.
V43|Vendor Search (`/vendor-search`): text search PN\|MFG P/N; scan panel shares decoder with Inventory — decode ok → search box = MFG P/N\|SKU + auto search, decode error → panel shows error, raw ECIA string never placed in search box. side-by-side Digikey+Mouser panes, multi-select cross-vendor, results+selection persist `sessionStorage`. `Add to Library` → prefilled draft (one primary source, all selections become distributor rows); `Append to Existing Parts` → exact MFG P/N match targets primary or alt, no match → modal to add as new alt on another part or fall back to new-part; footprint download via Ultra Librarian\|SnapEDA.
V44|ECO (`/eco`): status buckets `pending\|in_review\|approved\|rejected`; filters part\|ECO number\|initiator\|pipeline tags; row expand → lazy full detail; eligible approvers comment/approve/reject with reason; any authenticated ECO-visible user may inspect; per-ECO PDF export; current-stage actability respects explicit approver assignment + delegation.
V45|File Library (`/file-library`) access = `read-write\|approver\|admin` only; `lab` keeps the part-scoped picker in Library edit and never reaches this page. modes: `File Types` (schematic\|footprint\|pad\|3D\|PSpice, type counts, global search, all/orphans filter, per-user copy-path, PSpice section separates `.lib` libs from `.olb` symbols, footprint/pad/3D headers show linked counterparts, admin footprint header exposes `Edit Pad Link`/`Edit 3D Model Link`, orphan-only multi-select delete, CIS template/config downloads) and `Category` (category → component → files, same rename/copy-path/sharing rules).
V46|File Library shared rename: `CONFIG_ECO` on & actor ≠ admin & affected parts > 1 & ≥1 affected part status ≠ `new` → warning modal → single ECO tagged `shared_file_rename` → only non-`new` affected parts move to `reviewing` (`new` parts stay editable) → rename applies post-approval; admin renames directly even with ECO on (mirrors §V20).
V47|Inventory (`/inventory`): filters category\|project\|approval\|location; full-text search across part + alt fields (200ms debounce); sort PN\|MFG P/N\|qty\|location\|min\|last-edited, persisted `localStorage` `viewPrefs:inventory` (whitelist-validated); scan panel shares the Vendor Search decoder (`VendorBarcodeScanPanel`, Enter/Tab terminator → instant decode, 250ms fallback debounce, camera modal, keystrokes re-render panel only); scan decode not visible in filtered view → server lookup `POST /api/inventory/search/barcode` — hit hidden by filters → clear-filters CTA, no hit → Vendor Search CTA; table row expands to alts; edit-all supports bulk location, direct qty set, consume, receive, min updates for primary+alt.
V48|Projects (`/projects`): status `active\|completed\|archived`; rows show lowest-break unit price + extended line total when distributor pricing exists, header shows rolled-up priced total and flags missing pricing; add by search or bulk-pasted MFG P/N; `Consume All` decrements the whole project; `Generate BOM` opens a column-picker modal (admin defaults) → CSV, enriched with live component+distributor+alt data (§V17, `client/src/utils/bomExport.js`).
V49|Reports (`/reports`) live data + CSV export for: Library Quality, CAD Coverage By Category, Category Distribution, Estimated Inventory Value, Footprint Issues, Manufacturer Coverage, Low Stock. Audit (`/audit`): filter by search\|activity type\|date range\|page size, row expand shows JSON details, export filtered CSV, paginated.
V50|User Settings (`/user-settings`): account summary (username/role/sign-in method local\|SSO), profile (display name/email/file storage path), ECO notification prefs, delegated backup approver, password change local-only (SSO accounts see an IdP-managed note instead of the form).
V51|Admin Settings (`/admin-settings`) tabs: User (create/edit/activate/deactivate/delete, role change incl. `lab`, export/import user JSON); BOM (default export columns); Category (create/edit prefix+leading-zeros+name, global prefix rules, reorder, category specs, manufacturer rename/merge); ECO (number prefix+seq, approval stages/order/parallel groups/required roles/approvals/stage tags incl. `Shared Rename`/assigned approvers, export/import stages JSON, PDF branding, doc-control notify email); Email (SMTP host/port/auth/from/TLS, test connection/email, ECO template preview); Update (bulk stock/spec refresh oldest-sync-first per §V24, bulk distributor fill, library-folder scan for untracked CAD); Operation (verify schema, init defaults, export/import compressed DB backups, destructive full reset/delete parts\|project data\|users); Logs (clear audit log after confirm).
V52|end-to-end operator paths: vendor-assisted new part = Vendor Search/scan → multi-select → send to Library → review category/numbering/specs/distributors/alts/CAD → save or later ECO. manual new part = Library add manual → category+required fields → specs/CAD/alts → save (inventory row created/repaired). controlled change = open saved part → start ECO → stage changes+notes → review/act in ECO → rejected → reload change-set + resubmit. shared file governance = link/upload in Library → File Library inspect reuse/rename/orphans/copy paths → admin library scan for unregistered disk files. stock→BOM = maintain Inventory qty → build Projects from saved parts → adjust/bulk import MFG P/N → consume → export BOM with chosen columns.
V53|global UX rules: guest login is first-class read-only; limited roles (`read-only\|reviewer`) never see Dashboard/File Library/Inventory/Projects/Reports/Audit nav; ECO visibility is feature-flagged; CAD uploads stage before save and stay reversible until save/cancel; footprint pairs stay grouped across upload/rename/File Library; add/edit conflict modal offers reuse-existing\|overwrite; ECO mode never allows live-file overwrite; ECO shared non-admin rename always warns before staging (admin renames direct); file-path copy depends on per-user storage base; BOM export columns default from admin but stays user-selectable per export; approval-stage assignees narrow action to named users; delegation allows backup action.
