<!-- SPEC FORMAT (baked by /encode-docs — keep; makes this file self-describing)
Sections, fixed order: §G goal | §C constraints | §I interfaces | §R research? | §V invariants
Symbols: → leads to | ∴ therefore | ∀ every | ∃ some | ! must | ? may/unknown | ⊥ never | ≠ | ∈ | ∉ | ≤ | ≥ | & and | § section
Durable truth only. Mutable: add sparingly (high bar), prune freely on evidence.
Address §<S>.<n> — §V.2 = invariants item 2. Commits/PRs cite by §.
Encoding: drop articles/filler/aux verbs. Fragments fine. Short synonyms (fix > implement).
Preserve verbatim: code, paths, identifiers, URLs, numbers, error strings, SQL, regex.
Tables (§C/§I/§R/§V): pipe-delimited, id-keyed; header row + GFM delimiter row (|---|---|), one cell per column. Escape literal \| . Empty cell = -
ids: monotonic, never reused — take the next from `next:` below, ⊥ from the highest row (rows get pruned)
next: C14 I14 R26 V65
One file rule: >1000 lines → prune stale §V, ⊥ split into more files.
Full rules: /encode-docs skill. Cutting a word that loses a fact ⊥ allowed.
-->

# IC-Lib SPEC

## §G GOAL

G1: manage PCB/OrCAD component lib end-to-end: vendor intake -> CAD asset control -> approval/status control -> inventory/project/BOM reuse.
G2: keep app operable on fresh or drifted DB via startup init, numeric migrations, auth bootstrap, file scan/repair, email/runtime config.
G3: provide audit/reporting/admin surfaces for governed library ops.

## §C CONSTRAINTS

id|description
|---|---|
C1|stack React 19 + Vite + TailwindCSS v4 + React Query 5 \| Express 4 \| PostgreSQL 18 \| JWT cookie auth.
C2|DB primary keys ! `UUID DEFAULT uuidv7()`; create time derive via `created_at(id)`.
C3|app owns flat CAD tree `library/footprint\|symbol\|model\|pspice\|pad` + temp/delete buffers; DB tracks `cad_files` + `component_cad_files`, plus `footprint_related_cad_files` for reusable footprint-driven pad and 3D-model history.
C4|views `components_full`, `component_specifications_view`, `eco_orders_full`, `production_parts`, `prototype_parts`, `archived_parts`, `alternative_parts` = external OrCAD-CIS/ODBC compat surface only — server runtime ⊥ query them; keep + startup-verify all 7 (`EXPECTED_SCHEMA_VIEWS`, locked by `schemaInspectionService.test.js`); OrCAD/CIS compat ! keep TEXT cols `pcb_footprint\|schematic\|step_model\|pspice\|pad_file`. component-facing views `components_full\|component_specifications_view\|production_parts\|prototype_parts\|archived_parts\|alternative_parts` ! append component-default `alt_class` after existing columns; `eco_orders_full` unchanged. note: live `schema_migrations` holds historical row `0_schema_version_1_8_0.sql` (file ∉ repo) — inert, pending-detection = files minus rows; ⊥ delete history. note: `footprint_sources` table legacy — 0 rows live, ∄ writer (footprint fetch stages via `library/temp` instead); kept in schema/backup/clear lists, ⊥ new writes.
C5|roles `read-only\|reviewer\|lab\|read-write\|approver\|admin`; `lab` = `read-write` except File Library page/browse/manage access ⊥; some component-scoped CAD helper APIs still usable from Parts Library edit flow. role-gate implementation (`server/src/middleware/auth.js`): `authenticate`, `canWrite`, `canApprove`, `isAdmin`, `canDeleteLibraryFiles` (approver\|admin), `canAccessFileLibrary` (read-write\|approver\|admin), `canDirectEditComponent`/`canDirectEditComponentByBody` (ECO-mode edit policy per §V15); client mirror `client/src/utils/accessControl.js`. optional-actor read flows use `req.user?.id \|\| null`.
C6|runtime flags/env ! support `CONFIG_ECO`, `CONFIG_BASE_URL`, `CONFIG_SUBDIRECTORY_PATH`, DB creds, vendor API creds, `SMTP_ENCRYPTION_KEY`.
C7|startup may repair missing base schema from `database/init-*.sql`; incremental change ! live in `database/migrations/<int>_<desc>.sql`. `database/init-*.sql` ! fresh-init/full-rebuild only — ⊥ `ALTER`, backfill, constraint rewrite, legacy cleanup. migrations idempotent when practical (`IF NOT EXISTS`, guarded `DO $$`); startup auto-applies pending files. migration adding startup-required cols/views/tables ! update server schema inspection expectations too. release traceability = version in migration header + `CHANGELOG.md`, ⊥ filename. live DB (`flat.gentex.int:5434/iclib`) ⊥ agent-writable; validate migrations on scratch PostgreSQL cluster before landing.
C8|file ops & import/repair flows must work on shared repo filesystem; path escape ⊥.
C9|spec target code-as-built first; known drift/bugs tracked via `CHANGELOG.md` + git history, ⊥ hidden.
C10|flexible workflow/audit payloads may persist in JSONB: `activity_log.details`, distributor `price_breaks`, ECO alt `distributors`, similar variable-shape data.
C11|naming: camelCase vars/fns, PascalCase components, snake_case DB cols/tables. server files `{entity}Controller.js`\|`{name}Service.js`\|`{entity}.js` routes; client files `{PageName}.jsx` pages, `{ComponentName}.jsx` components. logs ASCII only, `[LEVEL] [ServiceName] Message`. UI: minimal icon use. line endings: tracked text ! LF in git, governed by `.gitattributes` (⊥ ad-hoc `core.autocrlf`); `*.sh` + container entrypoints (`start.sh`, `docker/repair`) ! `eol=lf` ∵ a CRLF shebang makes Linux exec `/bin/sh\r` → `not found`; tracked binaries + `*.reg` (UTF-16LE) ! declared `binary`, ⊥ normalized.
C12|inventory/project/dashboard read routes stay unauthenticated per §V10 allowlist — ⊥ add auth there unless a feature explicitly changes the access model.
C13|AD authentication path ! direct standards-based OIDC (primary: Microsoft Entra ID; AD FS remains generic-OIDC compatible), ⊥ Keycloak broker/federation, ⊥ app LDAP bind, ⊥ Windows/proxy auth. Non-AD deployments may configure any standards-compliant OIDC provider; provider-specific authorization claims ⊥ required.

## §I INTERFACES

id|type|shape → output
|---|---|---|
I1|ui|`/login\|/\|/library\|/file-library\|/inventory\|/vendor-search\|/projects\|/reports\|/audit\|/user-settings\|/admin-settings\|/eco?` -> React SPA; nav gated by role + `ecoEnabled`.
I2|api|`/api/auth/*` -> login/logout/verify, password change, profile, file-storage-path, ECO prefs, admin user CRUD; `/api/auth/oidc/status\|login\|callback` -> SSO flow (V29); authenticated requests re-check current local active state (V1).
I3|api|`/api/components/*\|/api/categories/*\|/api/manufacturers/*\|/api/distributors/*` -> catalog read, component CRUD, spec/distributor/alt-part CRUD, approval, category change, bulk vendor refresh, component-default alternative-class single/bulk writes, component project-assignment read for library view.
I4|api|`/api/search/*` -> Digikey/Mouser/all-vendor lookup, add-to-library draft seed, Ultra Librarian/SnapEDA footprint fetch.
I5|api|`/api/inventory/*` -> inventory read, low-stock, barcode search, alt inventory read/update, inventory CRUD.
I6|api|`/api/files/*\|/api/file-library/*` -> temp upload/finalize/cleanup/export, collision check, CAD browse/search/orphans/link/unlink/rename/delete/scan, admin Filename Sanitization run + report (V64).
I7|api|`/api/eco/*` -> ECO list/detail/create/delete/approve/reject/pdf/last-rejected + stage CRUD/import/export/approver assignment.
I8|api|`/api/projects/*` -> project CRUD, project component add/update/remove incl nullable per-line alternative-class override, consume inventory; project detail rows include resolved alternative class + lowest-break unit pricing when distributor data exists; client BOM export via `client/src/utils/bomExport.js`.
I9|api|`/api/dashboard/*\|/api/reports/*\|/api/settings/*\|/api/smtp/*\|/api/admin/*` -> stats, audit feed/clear, reports, feature flags, category/BOM/ECO/email/db ops, CIS/label downloads, SMTP test, admin init/reset/verify.
I10|cmd|`./start.sh` -> dev server; `./test.sh` -> lint + tests; `node scripts/import.js [--dry-run\|--file=<category>]` -> legacy CSV import; `cd server && npm run repair -- admin-reset` -> reset default `admin` password.
I11|env|`JWT_SECRET` ! set; `CONFIG_ECO`, `CONFIG_BASE_URL`, `CONFIG_SUBDIRECTORY_PATH`, `DB_*`, vendor API creds, `SMTP_ENCRYPTION_KEY`; optional `OIDC_ISSUER_URL\|OIDC_CLIENT_ID\|OIDC_CLIENT_SECRET\|OIDC_REDIRECT_URI\|OIDC_SCOPES\|OIDC_PROVIDER_NAME\|OIDC_DEFAULT_ROLE`; optional `OIDC_ALLOWED_TENANTS` (CSV `tid` allowlist). SSO needs issuer+client id + valid §V55 tenant config. optional SCIM lifecycle needs both `SCIM_TENANT_ID` + secret `SCIM_BEARER_TOKEN`; partial/invalid config ! fail startup; IdP authorization-claim config ∄ (§V57).
I12|api|`/api/scim/v2/ServiceProviderConfig\|ResourceTypes\|Schemas\|Users` -> bearer-authenticated single-tenant Entra SCIM discovery + existing-linked-user lookup/profile/active lifecycle; Users create for unknown identity ⊥ (V60); Groups/Bulk ⊥.
I13|api|`/api/packages/*` -> built-in package catalog read (canonical short name, family, mount, `count_policy`, aliases), alias->canonical resolve, admin package/alias CRUD (V62).

## §R RESEARCH

id|claim|source
|---|---|---|
R1|Entra v2.0 ID-token claim reference ∄ `email_verified`; generic OIDC verified-email linking therefore ⊥ link Entra users. Enabling trust in unverified `email` would turn mutable profile data into an account-link credential.|https://learn.microsoft.com/en-us/entra/identity-platform/id-token-claims-reference (checked 2026-07-29)
R2|Entra `email` present by default only for guest accts w/ email; managed users need `email` optional claim. Value mutable & explicitly ⊥ authorization key. Stable identity = (`iss`,`sub`); cross-client continuity = (`iss`,`tid`,`oid`) because `sub` pairwise per app while `oid` stable across apps inside tenant.|https://learn.microsoft.com/en-us/entra/identity-platform/id-token-claims-reference (checked 2026-07-29)
R5|Entra tenant-independent metadata (`/common`\|`/organizations`) returns templated issuer; validation ! substitute GUID `tid`, exact-match `iss`, validate signing-key issuer, & scope identity data by tenant. Installed `openid-client` `6.8.4` handles `login.microsoftonline.com` templated issuer; `6.8.4` latest release.|https://learn.microsoft.com/en-us/entra/identity-platform/access-tokens + `server/node_modules/openid-client/build/index.js:174-179,286-298,491-494` + https://github.com/panva/openid-client/discussions (checked 2026-07-29)
R6|`openid-client` `TokenEndpointResponseHelpers.claims()` returns validated ID-token JWT payload typed w/ arbitrary claims; `iss`\|`sub`\|`aud`\|signature\|nonce validation remains library-owned. App may read optional `tid`\|`oid` identity extensions while ignoring `roles`\|`groups`\|`wids`.|`server/node_modules/openid-client/build/index.d.ts:1108-1122`; `server/node_modules/oauth4webapi/build/index.d.ts:1742-1763`; https://openid.net/specs/openid-connect-core-1_0.html (checked 2026-07-29)
R7|ESLint 9 `no-shadow` reports catch-clause params shadowing an outer binding (`no-catch-shadow` removed from flat config); plain `'no-shadow': 'error'` suffices, ⊥ options. scope matters: server `src/**/*.js` → exactly 19 errors = the logger-shadow set w/ ∄ collateral; client `src/**/*.{js,jsx}` → 7 unrelated pre-existing (`AuthContext.jsx:47` `user`, `Inventory.jsx:150` `location`, `Library.jsx:732\|1115\|1586\|2359` `distributors`, `Library.jsx:2419` `response`); scripts → 0.|installed `server/node_modules/eslint` v9.39.4 + fixture run (checked 2026-07-29)
R8|`express-rate-limit` 7.5.1 gives each `rateLimit()` call its own `new MemoryStore()` when ∄ explicit `store` ∴ separate limiter instances isolate budgets. default key = client IP; custom `keyGenerator` may use authenticated user id. default MemoryStore is process-local + resets on restart ∴ multi-instance enforcement needs an external shared store.|`server/node_modules/express-rate-limit/dist/index.mjs:627-632,650`; https://express-rate-limit.mintlify.app/reference/configuration; https://express-rate-limit.mintlify.app/reference/stores (checked 2026-07-29)
R9|PostgreSQL statement error inside an explicit txn leaves that txn unable to proceed normally. Recovery while retaining earlier work requires a savepoint established before the risky statement + `ROLLBACK TO SAVEPOINT`; otherwise roll back the whole txn. `RELEASE SAVEPOINT` itself is invalid while txn is aborted.|https://www.postgresql.org/docs/current/sql-rollback-to.html; https://www.postgresql.org/docs/current/sql-release-savepoint.html (checked 2026-07-29)
R10|readiness failure is a traffic-routing signal, ≠ process restart: Kubernetes removes an unready Pod from matching Service EndpointSlices while continuing readiness probes; liveness failure governs restart. ∴ `/ready` must test required serving dependencies, while `/health` remains cheap + DB-free.|https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#container-probes (checked 2026-07-29)
R11|Microsoft Entra application provisioning targets a SCIM 2.0 endpoint for create/update/remove lifecycle. Disable/out-of-scope normally arrives as user `active=false`; Microsoft recommends target support for both soft + hard delete. ∴ IC-Lib can map SCIM disable to retained local `users.is_active=false`, but endpoint auth, tenant scope, assignment scope, replay, & reactivation remain app policy.|https://learn.microsoft.com/en-us/entra/identity/app-provisioning/how-provisioning-works; https://learn.microsoft.com/en-us/entra/identity/app-provisioning/use-scim-to-provision-users-and-groups (checked 2026-07-29)
R12|Microsoft Graph `GET /users/delta` returns initial + incremental user creates/updates/deletes via persisted `@odata.nextLink`/`@odata.deltaLink`; application least privilege listed = `User.Read.All`. ∴ Graph deprovision is a polling/checkpoint worker w/ broader directory-read + eventual-sync operations, ≠ push endpoint.|https://learn.microsoft.com/en-us/graph/api/user-delta?view=graph-rest-1.0; https://learn.microsoft.com/en-us/graph/delta-query-users (checked 2026-07-29)
R13|Entra SCIM first queries its configured matching property and expects `200` + empty `ListResponse` when absent, then `POST /Users`; Test Connection performs that query with a random GUID using the selected matching property. disable uses case-insensitive PATCH `Replace active=false`; hard deprovision may use DELETE. mutation bodies + responses use `application/scim+json`; SCIM auth is deployment policy, with a long-lived secret token supported by Entra. incremental sync is external/eventual (~40 min documented cadence). ∴ IC-Lib's OIDC-first policy must return unknown lookup empty but reject subsequent unknown create explicitly, document first-login/retry, support active PATCH + soft DELETE, configure immutable Entra `objectId -> externalId` matching, and describe cutoff as immediate after local deactivation—not guaranteed Entra delivery time.|https://learn.microsoft.com/en-us/entra/identity/app-provisioning/use-scim-to-provision-users-and-groups; https://www.rfc-editor.org/rfc/rfc7644.html (checked 2026-07-29)
R14|PostgreSQL 18 `CREATE OR REPLACE VIEW` replacement columns must retain existing names/order/types; only new columns at list end are allowed. `ALTER TABLE ADD COLUMN` without a default yields NULL for existing rows. ∴ `alt_class` view exposure must append after every existing projection and scratch migration tests must compare ordinal prefixes, not only view existence.|https://www.postgresql.org/docs/18/sql-createview.html; https://www.postgresql.org/docs/18/sql-altertable.html (checked 2026-07-29)
R15|Git `text=auto` normalizes detected text; `eol=lf` fixes checkout EOL; adoption flow = policy first + `git add --renormalize .`; paths detected binary need `-text`. Linux shebang parser ends @ LF and trims only space/tab ∴ CR before LF remains in interpreter pathname (`/bin/sh\r`).|https://git-scm.com/docs/gitattributes; https://github.com/torvalds/linux/blob/master/fs/binfmt_script.c (checked 2026-07-31)
R16|Express numeric `trust proxy=0` means direct socket address; `n>0` trusts forwarded hops and is unsafe when a client can reach app by a shorter path. `express-rate-limit` keys default IP limits from Express client IP ∴ direct deployment must default 0; bundled nginx may explicitly set 1.|https://expressjs.com/en/guide/behind-proxies/; https://express-rate-limit.mintlify.app/guides/troubleshooting-proxy-issues (checked 2026-07-31)
R17|SCIM POST ! ignore read-only attrs & success → 201 + resource `Location`; PATCH ! reject read-only/immutable mutation; SCIM errors use SCIM JSON; individual advertised Schema/ResourceType locations are retrievable. HTTP auth schemes are case-insensitive. Entra sends mutation `application/scim+json` and stock create may include `meta` + empty `roles`. ∴ ingress auth precedes parsing/fallback; POST read-only/default no-ops ≠ locally-owned sensitive mutation refusal.|https://www.rfc-editor.org/rfc/rfc7644.html; https://www.rfc-editor.org/rfc/rfc9110.html; https://learn.microsoft.com/en-us/entra/identity/app-provisioning/use-scim-to-provision-users-and-groups (checked 2026-07-31)
R18|IPC-7351B/Library Expert density suffix = last char of name: `M` = Most Material Condition (Density Level A), `N` = Nominal (Level B), `L` = Least (Level C) ∴ legacy `_m\|_n\|_l` → `_A\|_B\|_C` is a semantic remap, ⊥ alphabetic (`_l`→`_C`, `_m`→`_A`, `_n`→`_B`). BGA family carries ⊥ density suffix (nominal only). name grammar = family prefix (first 3-6 chars) + pitch `P` + dims joined by `X` + `-` + pin qty + density char; `-` separates pin qty; `_` separates hidden/deleted pin qty & appends modifiers; ∀ dims metric, 2 decimal places, ⊥ leading zeros. dim-code letters: `P` pitch, `W` lead width, `L` body length, `D` diameter, `T` thickness, `H` height, `Q` pin qty, `R` rows.|`F:\DevPCB\Reference\IPC\IPC-7351B-Land-Pattern-Naming-Convention_0281365.pdf` p1-3,5 (PCB Libraries Library Expert, rev 2016-10-04; extract `pdftotext -layout`) (checked 2026-08-03)
R19|SOT alias equivalence sets: SOT23-3 = TO-236AB\|TO-236AA\|SOT346\|SC59A\|SMT3; SOT323 = SC70-3\|UMT3; SOT416 = SOT523\|SC75A\|SC90\|EMT3; SOT23-5 = SOT25\|SC74A\|TSOP-5\|SOT753\|MO-178AA\|SMT5; SOT353 = SC70-5\|SC88A\|TSSOP-5\|UMT5; SOT23-6 = SOT26\|SC59-6\|SC74\|TSOP-6\|MO-178AB\|SMT6\|SM6\|Mini6\|SOT457; SOT363 = SC70-6\|SC88\|TSSOP-6\|UMT6\|US6\|S-Mini6; SOT563 = SC-107C\|ES6; SOT23-4 = SOT143\|TO253; SOT490 = SOT523F\|SC89\|EMT3F; SOT89-3 = TO243AA\|SC62\|MPT3; SOT223-4 = SC73\|TO261AA; SOT223-8 = SM-8; SOT23-8 = SOT28; SOT54 = TO-92. ∴ alias graph is many-to-one & spans JEDEC `TO-`, EIAJ `SC-`, JEITA `SOTnnn`, & vendor house codes ∴ a single canonical short name ! own N aliases (V62).|https://en.wikipedia.org/wiki/Small-outline_transistor (checked 2026-08-03)
R20|diode/power alias sets: DO-214AC=SMA, DO-214AA=SMB, DO-214AB=SMC, DO-214BA=GF1; DO-41=DO-204AL, DO-35=DO-204AH, DO-15=DO-204AC=SC-39, DO-34=SOD68=MSD; DO-213AA=MiniMELF=SOD-80=LL-34, DO-213AB=MELF=SOD-106=LL-41; SOD-123 > SOD-323 > SOD-523 > SOD-723 > SOD-923 (descending size, same family); TO-252=DPAK, TO-263=D2PAK=DDPAK=SOT404, TO-251=IPAK, TO-262=I2PAK, TO-268=D3PAK, TO-273=Super-220, TO-274=Super-247, TO-277=SMPC=SM-7. ∴ diode & power families alias across DO-/SOD-/SM-/LL-/vendor-nickname namespaces w/ ∄ pin count in the canonical name.|https://en.wikipedia.org/wiki/DO-214; https://en.wikipedia.org/wiki/TO-252; https://en.wikipedia.org/wiki/TO-263; https://blog.mbedded.ninja/pcb-design/component-packages/minimelf-component-package/; https://www.topdiode.com/pdf/Size(Package)CodeCrossReference.pdf (checked 2026-08-03)
R21|TI packaging terminology gives family abbreviation + explicit aliases: DSBGA=WCSP, SON=DFN, VSSOP=MSOP. modifier grammar is prefix-composed on a base family: `T`=Thin, `V`=Very thin, `L`=Low profile, `S`=Shrink, `H`=thermally enhanced, `C`=Ceramic, `P`=Plastic, `M`=Metal ∴ HTSSOP\|HSSOP\|HSOP\|HTQFP\|HVQFP\|HLQFP\|HQFP\|LQFP\|TQFP\|VQFP\|MQFP\|CDIP\|PDIP\|SDIP derive mechanically from QFP\|SOP\|DIP bases. ∴ catalog ! store composed short names as first-class rows, ⊥ synthesize by prefix rules at runtime.|https://www.ti.com/design-development/packaging/packaging-terminology.html (checked 2026-08-03)
R22|Vishay land-pattern drawings label parts in `FAMILY-PINCOUNT` house form (SOIC-8, SOIC-14, SOIC-16, SOIC-20, DFN-8, QFN-20, SC70-3, SC70-4, VSSR-16, VTSR-20) ∴ trailing-pin-count matches an existing manufacturer convention. JEDEC MO- outline refs bind families: MSOP=MO-187AA, SSOP=MO-137, TSSOP=MO-153 ∴ `MO-` codes are alias keys alongside `TO-`/`SC-`.|`F:\DevPCB\Reference\Vishay\Vishay landpatterns.pdf` p3,5 (rev 24-Aug-2021, doc 60119) (checked 2026-08-03)
R23|analog.com package index (user-cited) ⊥ fetchable from this environment — 3 attempts, 2x `read ECONNRESET` + 1x `timeout of 60000ms exceeded`; web-search surfaces only the landing page + per-package PDFs (`.../pkg_pdf/lfcspcp/cp-8/CP_8_13.pdf`), ∄ machine-readable index. status = CLOSED as an accepted gap, ⊥ retried again. ADI house codes (LFCSP/`CP-nn`, `CC`, `RQ`, `RU`) stay unconfirmed & are catalog rows an admin may add (V62).|https://www.analog.com/en/resources/packaging-quality-symbols-footprints/package-index.html (3 attempts, last 2026-08-03)
R24|`package_size` provenance: client seeds it from the vendor `Package / Case` spec & falls back to `vendorData.packageType` (`client/src/pages/Library.jsx:639-649`, mirrored `:2674-2693`). DigiKey builds specs from `product.Parameters[].ParameterText -> {value: ValueText}` (`server/src/services/digikeyService.js:162-168`) & `packageType` from `primaryVariation?.PackageType?.Name` (`:172`) — a per-ProductVariation field, ⊥ a per-product one; DigiKey PIV4 `ProductVariations[].PackageType` = `{Id,Name}`. Mouser sets `packageType` from `part.PackageType \|\| 'N/A'` (`server/src/services/mouserService.js:65`). ∴ the fallback can put a non-package string (incl. `'N/A'`) into `package_size` ∴ alias resolution ! be miss-tolerant (V62 pass-through), ⊥ force-fit. exact `PackageType.Name` value domain unconfirmed — developer.digikey.com portal ⊥ machine-fetchable.|`client/src/pages/Library.jsx:639-649`; `server/src/services/digikeyService.js:162-172`; `server/src/services/mouserService.js:65`; https://developer.digikey.com/products/product-information-v4/productsearch/productdetails (checked 2026-08-03)
R25|PostgreSQL STORED generated column: "The generation expression can only use immutable functions and cannot use subqueries or reference anything other than the current row"; a non-immutable expression is rejected at DDL time w/ `generation expression is not immutable`. `lower(regexp_replace(alias, '[^A-Za-z0-9]', '', 'g'))` accepted in scratch PostgreSQL 18 on 2026-08-03; duplicate folded key + `count_policy='bogus'` rejected. ∴ folded-key generated column legal here; ⊥ application-computed drift.|https://www.postgresql.org/docs/18/ddl-generated-columns.html (checked 2026-08-03)

## §V INVARIANTS

id|invariant definition
|---|---|
V1|`JWT_SECRET` ! set; auth token via HttpOnly cookie `AUTH_COOKIE_NAME` 24h or Bearer fallback. every protected request ! verify JWT then read current `users.is_active` + `users.role` by token user id (1 query); missing user\|inactive\|missing\|invalid\|expired token -> 401 + ⊥ downstream handler; active-check DB failure -> generic 503 (fail closed), ⊥ trust stale JWT active state. live DB `role` ! win over the JWT `role` claim ∀ downstream gate (§V2, §V15) ∴ demotion\|elevation binds on the next protected request, ⊥ wait ≤24h token expiry; JWT `role` claim informational only.
V2|role gates hold: `read-only` ⊥ write; `reviewer` ⊥ write & ∈ ECO approval; `lab\|read-write\|approver\|admin` ∈ lib/project/inventory mutate; `lab` ⊥ File Library page/browse/manage routes but may use component-scoped CAD helper APIs during Parts Library edit; `approver\|admin` ∈ hard CAD delete; `admin` ∈ admin/user/settings mutate.
V3|default client route: limited role -> `/eco` if eco flag on else `/library`; full role -> `/`.
V4|startup detects blank DB = ∄ all of `component_categories\|manufacturers\|distributors\|components`. blank -> run `database/init-schema.sql` then migrations; existing (≥1 critical table) -> migrations only, ⊥ re-init (partial DB still missing objects -> boot fail @ schema inspection, ⊥ silent half-init). `database/init-users.sql` runs only when `users` table ∄. `database/init-settings.sql` idempotent-seeds every boot (ON CONFLICT DO NOTHING), ≠ init. migrations apply numeric order w/ full server log: found/applied/pending counts + per-file apply/complete + executed summary. post-migration missing required table/view/col -> boot fail. ⊥ inline `ALTER` in app code; cad_files.missing owned by init-schema + migration 1.
V5|migration filename contract: new files `database/migrations/<int>_<desc>.sql`; sort by int, not lexicographic; legacy zero-pad still parse numeric.
V6|component status ∈ `{new,reviewing,prototype,production,archived}`; ECO status ∈ `{pending,in_review,approved,rejected}`; create time read from `created_at(uuid)`.
V7|component create ! inventory row ∃, `activity_log` row ∃, joined API payload include category/manufacturer/part_type/created_at.
V8|CAD source-of-truth -> `cad_files` + `component_cad_files`; TEXT CAD cols derived by regen, strip ext, skip `.dra`; startup scan registers untracked files & toggles `cad_files.missing` from disk state; footprint-driven reuse history persists in `footprint_related_cad_files`, auto-links known pad and 3D model files when a footprint is linked, and learns new footprint/pad/model co-usage from component saves, ECO applies, and direct file linking; PSpice uses the existing `pspice` CAD category for both `.lib` libraries and PSpice `.olb` symbols stored under `library/pspice`, while ambiguous staged `.olb` uploads default to schematic first and may be reassigned to PSpice before finalization.
V9|fs ops ! safe leaf names + resolved path ∈ library base; traversal/nested path reject.
V10|public-read boundary is an explicit allowlist, ⊥ blanket: unauthenticated GET only for catalog (`components\|categories\|manufacturers\|distributors\|packages` reads), inventory reads (+ `POST /api/inventory/search/barcode` mutation-shaped read), project reads, `reports/*`, dashboard `stats\|recent-activities\|category-breakdown\|extended-stats`, settings reads (`/features\|/\|/eco\|/eco/logo\|/eco/preview\|/global-prefix\|/categories(+specs)`, CIS/label downloads), SSO flow `GET /api/auth/oidc/status\|login\|callback` (pre-session by design, V29) — the guest read-only surface. ∀ other GET ! `authenticate` (+role gate matching consuming page): audit feed `GET /dashboard/activities/all` -> auth; `GET /dashboard/db-info` -> auth; `GET /settings/database/status\|verify` -> auth+admin; eco/fileLibrary/fileUpload/auth/admin/smtp readers stay guarded. allowlist enforced 2-way (unlisted public GET & stale allowlist entry both fail) by `routeAuthGuards.test.js`; adding a public GET ! update test allowlist & this invariant. optional-actor read flows use `req.user?.id \|\| null`.
V11|ECO routes auth ! always; any auth user may view; create -> `canWrite`; approve/reject -> `canApprove`; current-stage actability computed per user + stage.
V12|ECO pipeline tags ∈ `{proto_status_change,prod_status_change,spec,filename,shared_file_rename,distributor,alt_parts}`; legacy `general\|spec_cad\|status_change` normalize; stage match needs status-tag match & detail-tag match.
V13|ECO vote uniqueness ! one effective vote per `(eco_id, stage_id, COALESCE(acting_for_user_id,user_id))`; delegation target ! same\|higher role; delegated user may consume assigned approver slot.
V14|rejected ECO retains lineage via `parent_eco_id`; retry UI may preload rejected field/spec/distributor/alt/CAD deltas + status proposal under same ECO number chain.
V15|Library edit policy: `CONFIG_ECO` on -> admin may direct-edit any part incl direct status reset; non-admin write roles may direct-edit only `approval_status=new` parts while keeping status `new`; controlled parts `reviewing\|prototype\|production\|archived` require ECO for direct field/spec/distributor/alt/category/CAD-link/delete changes. ECO mode blocks live CAD overwrite & stages category/status/field/spec/distributor/alt/CAD changes + notes.
V16|notifications optional: SMTP disabled -> send skip; enabled -> welcome/ECO/doc-control emails use `CONFIG_BASE_URL` fallback chain, user prefs from `email_notification_preferences`, delegation from `users.delegation`, send/fail log in `email_log`.
V17|project/inventory contract: project mutation routes auth+canWrite; inventory mutation routes auth+canWrite; project detail pricing + inventory-value reporting derive from the lowest-quantity distributor price break, using the cheapest matching unit price across distributors; project detail resolves alternative class per §V59; BOM export client-side from project detail + live component/distributor/alternative fetch.
V18|fresh DB repo schema ! cover every persisted API surface present in §I.
V19|fresh DB default ECO stage config ! include every runtime pipeline tag required by §V12.
V20|file-library shared rename policy: actor = `admin` -> direct rename even if file shared; `CONFIG_ECO` on & actor ∉ `admin` & affected parts > 1 & any affected part status != `new` -> shared transparent-gray warning before submit, stage 1 ECO w/ `shared_file_rename` tag, set only affected non-`new` parts `approval_status=reviewing`, approve -> rename shared CAD files, refresh CAD text for staged + skipped `new` parts, restore staged parts original status; reject\|delete -> restore staged parts original status & keep file info unchanged.
V21|ECO status proposal UI/API ! allow `new -> prototype`, `reviewing -> archived`, `prototype -> production\|archived`, `production -> prototype\|archived`, `archived -> prototype\|production`; ECO path back to `new` ⊥; `new`-part ECO without `prototype` proposal ⊥. (truth: `client/src/utils/ecoStatusProposalOptions.js`)
V22|ECO CAD diff/apply key on persisted `file_type`; same-ext `.olb` in `symbol` vs `pspice` ! stay isolated through ECO submit/approve, so link/unlink in one role ⊥ mutate the other role by filename/ext coincidence.
V23|vendor-search append flows ! normalize optional FK lookups before component/alternative distributor writes: blank `manufacturer_id\|distributor_id` -> `null`/skip; alternative create may resolve/create manufacturer by `manufacturer_name` when local ID miss.
V24|admin bulk vendor refresh queues oldest sync first: stock -> supported distributor rows by `distributor_info.last_vendor_sync_at`; specs -> DigiKey-backed components by `components.last_specs_refresh_at`; non-rate-limit completion bumps cursor even when vendor data missing; vendor daily-limit reject -> abort batch @ current item.
V25|CAD fs mutation ! atomic across disk+DB. rename (`renameCadFile`, file-library single-file rename, part-edit `renameFile`, ECO `applyMassFileRenameEco`, `renameFootprintGroup`): physical rename + `cad_files` update + TEXT regen ∈ 1 txn; fail -> DB rollback + best-effort physical revert. delete (`deleteCadFile`): DB row delete + TEXT regen ∈ txn, unlink only post-COMMIT (crash -> harmless on-disk orphan re-surfaced by scan, ⊥ DB row -> missing file). same-inode case-only rename ⊥ collision reject. temp finalize move-then-register self-heals via scan.
V26|component CAD single-file slots ! ≤1 per component: schematic symbol, 3D model, PSpice symbol (`.olb`). PSpice `.lib` libraries multi-allowed. slot keyed by `getCadFileSlot(category,filename)` (role-aware for pspice via `getPspiceFileRole`). upload\|link of 2nd file into an occupied slot -> keep-vs-replace conflict modal (`client/src/components/library/ComponentFiles.jsx`, mirrors symbol/model guard); ECO CAD diff stays `file_type`-isolated per §V22.
V27|∀ state-changing route (POST\|PUT\|PATCH\|DELETE) ! an explicit authentication gate first: app-user routes use `authenticate` + role gate matching resource write policy (§V2); `/api/scim/v2/*` uses `authenticateScim` only (§V60). public exceptions only: `POST /api/auth/login`, `POST /api/inventory/search/barcode` (mutation-shaped read). enforced by full-router sweep `server/src/test/routeAuthGuards.test.js`; runtime app mounting + public-path resolver + sweep ! consume one ordered route registry, ⊥ hand-kept parallel maps ∴ adding a mounted unguarded router makes the sweep FAIL.
V28|footprint filenames (`.psm`\|`.bsm`\|`.dra`) normalize @ ∀ real input boundary (upload, ZIP extract, temp finalize, part-edit rename, File Library single+pair rename, mass-rename ECO staging): whole name lowercase & base dot-free (ext = last-dot segment, every other `.` silently dropped). non-footprint CAD case policy → §V63; canonical package identity → §V61. `+` -> typed `FootprintNameError` -> 422 + client pre-submit toast, ⊥ silent strip. legacy on-disk names grandfathered (scan registers as-is; use_existing links w/o reject). collision checks fire on normalized name -> 409, ⊥ silent overwrite. server truth `server/src/utils/footprintFiles.js`; client mirror `client/src/utils/footprintFiles.js` ! user-visible boundary-behavior parity. internal helper behavior on synthetic/invalid dotfile or outer-whitespace inputs need not match.
V29|OIDC/SSO federates identity @ login only: callback -> same app JWT cookie as local login ∴ `authenticate`/role gates/client AuthContext untouched; IdP ID/access tokens ⊥ app session; ⊥ server-side session store. primary identity = (`oidc_issuer`,`oidc_sub`) partial-unique. optional continuity identity = (`oidc_issuer`,`oidc_tenant_id`,`oidc_object_id`) partial-unique when `tid`+`oid` emitted; identity miss may relink by this exact triple, preserving local user id/role (§R2); primary + continuity keys resolving to different local users -> reject, ⊥ auto-merge. resolution: primary match -> continuity match -> exactly 1 non-federated local account w/ `email_verified===true` -> link -> else JIT local shadow; unverified email ⊥ link & ∄ bypass flag. JIT writes `auth_provider='oidc'`, `password_hash=NULL`, `OIDC_DEFAULT_ROLE` (default `read-only`). ∀ `auth_provider='oidc'` rows ! `password_hash=NULL` via DB CHECK; verified-email link atomically clears hash while setting provider/OIDC keys. local login/change-password/admin-password-set ! require `auth_provider='local'` before bcrypt/hash/write; provider gate remains authoritative against anomalous non-NULL OIDC data. inactive -> reject ∀ paths. Auth Code+PKCE via `openid-client` v6: state/nonce/verifier in JWT-signed httpOnly cookie 10m; callback errors -> `/login?error=sso_failed\|account_disabled`, ⊥ bare 500. enabled iff `OIDC_ISSUER_URL`+`OIDC_CLIENT_ID` + valid §V55 tenant config; local login stays break-glass. truth `server/src/services/oidcService.js`; locked by `oidcService.test.js`+`oidcController.test.js`.
V30|liveness ≠ readiness. liveness = process up (cheap, ⊥ DB). readiness = can serve = DB reachable + schema verified; DB unreachable -> readiness non-2xx (503), ⊥ 200. orchestrator probe (Dockerfile HEALTHCHECK / compose / k8s) ! consume readiness. `/api/health` ⊥ emit `status:'OK'` when DB down.
V31|process resilience. pool idle-client `error` -> log + evict, ⊥ `process.exit` (recoverable: failover/blip/idle-reset). `uncaughtException`\|`unhandledRejection` -> log FATAL + graceful shutdown + exit≠0. SIGTERM\|SIGINT -> stop accept (`server.close`) + await in-flight ≤ bounded timeout + `pool.end()` + exit 0. termination reserved for unrecoverable-startup \| signal, ⊥ transient backend hiccup.
V32|three isolated limiter stores/budgets: `POST /api/auth/login` failed attempts keyed by client IP; authenticated change-password failed attempts keyed by `req.user.id`; exact §V10 public allowlist only keyed by client IP @ default `1000/15m`. exceed -> 429. authenticated/private endpoints ⊥ consume public budget; login ⊥ consume change-password/public budget. checked-in one-app-container deployment may use process-local MemoryStore; multi-process/replica deployment ! shared external store before claiming uniform enforcement. env tunes each window/ceiling. `TRUST_PROXY_HOPS` ! integer ≥0, default 0/direct; bundled nginx image explicitly 1; direct client XFF ⊥ choose `req.ip` or rotate a limiter key.
V33|`project.status ∈ {active,completed,archived}`; reject out-of-domain @ API boundary (400) + DB CHECK constraint (parity w/ `components.check_approval_status`, `eco_orders.check_eco_status`). dashboard active-count ⊥ count undocumented statuses (`planning` phantom removed).
V34|outbound vendor HTTP (DigiKey\|Mouser\|footprint fetch) ! bounded `timeout`; hung upstream -> per-item skip/abort, ⊥ stall bulk batch (§V24) or hold request open ∞.
V35|`/login` = local sign-in + guest read-only sign-in + `Sign in with {provider}` button when OIDC on (`GET /api/auth/oidc/status`); SSO callback error → `/login?error=sso_failed\|account_disabled`; success → requested route if any. post-login default: `read-only\|reviewer` → `/eco` if ECO flag on else `/library`; `lab\|read-write\|approver\|admin` → `/`.
V36|nav by role: `read-only` = Library+ECO; `reviewer` = +User Settings; `lab\|read-write\|approver\|admin` = +Dashboard/Inventory/Vendor Search/Projects/Reports/Audit; `read-write\|approver\|admin` = +File Library; `admin` = +Admin Settings. ECO nav item shown only when ECO flag on. sidebar collapse state + dark mode persist `localStorage`; sidebar shows app version, user card, logout.
V37|UI model: most screens = left filter/nav pane + right detail pane; write flows use modals; toasts carry save/import/update progress. cross-page pivots: Library ↔ Inventory\|Projects\|Vendor Search\|File Library(role-gated), Inventory → Library\|Vendor Search, File Library → Library, Vendor Search → Library, Projects BOM settings → Admin BOM, User file-path → File Library copy-path, Admin scan → File Library new-file visibility.
V38|Dashboard (`/`, full-nav roles only): DB-not-initialized state shows warning + routes admin to settings; shows totals (library/categories/manufacturers/projects/distributors), approval-state counts, stock counts, category distribution, DB info, CAD-health.
V39|Library browse (`/library`): category sidebar, full-text search, sort by PN\|MFG P/N\|value\|desc\|created\|updated, multi-status filter, prev/next step when search parses as PN, virtualized list, copy deep link. view state persists: sort/order/status-filter/category → `localStorage` `viewPrefs:library` (whitelist-validated via `client/src/utils/viewPrefs.js`; corrupt/unknown → defaults; stale category id → reset to All Categories); search term → per-tab `sessionStorage` so add-part round-trips return to the same view.
V40|Library detail: copyable fields, alt selector, datasheet link, approval status/history, assigned-project tile (click → Projects), pivots to Inventory\|Add-to-Project\|File Library(role); CAD links route to File Library deep links only for File-Library roles (`read-write\|approver\|admin`) — `lab` sees plain filenames; distributor panel links to Vendor Search; specs shown read-only.
V41|Library add/edit: manual fields incl component-default alternative class (§V59), category numbering/template, manufacturer typeahead+create-new, package/sub-category suggest, spec editor + custom spec + vendor-field mapping, alt editor (promote/delete + distributor rows), CAD manager (schematic\|footprint\|pad\|3D\|PSpice) with file or ZIP upload, staged `.olb` → two-panel assignment popup (defaults schematic, may move to PSpice before save), link-existing-file, footprint link auto-adds known pad+3D from history, rename shortcuts (freeform\|MFG P/N\|package), footprint pairs move together, save-time conflict modal `use_existing\|overwrite`. detail/list show resolved display `Class A\|Class B\|Class C\|Unrated`; multi-select bulk-set obeys §V15 (ECO on: admin any, non-admin only `new`; controlled non-admin changes use ECO). vendor-assisted entry: Vendor Search opens add mode with prefilled payload, vendor panel `Auto Fill` maps base fields + mapped specs.
V42|Library direct-edit policy mirrors §V15 in the UI: ECO off → write roles edit live + bulk-delete; ECO on → admin edits any part incl. status reset, non-admin write roles edit only `approval_status=new` parts. ECO staging: write roles `Initiate ECO` on `new` parts only when proposing `prototype`; stage field/spec/distributor/alt/CAD/status/delete + notes per §V21; retry reloads rejected change-set under same ECO chain; ECO mode blocks live-file overwrite.
V43|Vendor Search (`/vendor-search`): text search PN\|MFG P/N; scan panel shares decoder with Inventory — decode ok → search box = MFG P/N\|SKU + auto search, decode error → panel shows error, raw ECIA string never placed in search box. side-by-side Digikey+Mouser panes, multi-select cross-vendor, results+selection persist `sessionStorage`. `Add to Library` → prefilled draft (one primary source, all selections become distributor rows); `Append to Existing Parts` → exact MFG P/N match targets primary or alt, no match → modal to add as new alt on another part or fall back to new-part; footprint download via Ultra Librarian\|SnapEDA.
V44|ECO (`/eco`): status buckets `pending\|in_review\|approved\|rejected`; filters part\|ECO number\|initiator\|pipeline tags; row expand → lazy full detail; eligible approvers comment/approve/reject with reason; any authenticated ECO-visible user may inspect; per-ECO PDF export; current-stage actability respects explicit approver assignment + delegation.
V45|File Library (`/file-library`) access = `read-write\|approver\|admin` only; `lab` keeps the part-scoped picker in Library edit and never reaches this page. modes: `File Types` (schematic\|footprint\|pad\|3D\|PSpice, type counts, global search, all/orphans filter, per-user copy-path, PSpice section separates `.lib` libs from `.olb` symbols, footprint/pad/3D headers show linked counterparts, admin footprint header exposes `Edit Pad Link`/`Edit 3D Model Link`, orphan-only multi-select delete, CIS template/config downloads) and `Category` (category → component → files, same rename/copy-path/sharing rules).
V46|File Library shared rename: `CONFIG_ECO` on & actor ≠ admin & affected parts > 1 & ≥1 affected part status ≠ `new` → warning modal → single ECO tagged `shared_file_rename` → only non-`new` affected parts move to `reviewing` (`new` parts stay editable) → rename applies post-approval; admin renames directly even with ECO on (mirrors §V20).
V47|Inventory (`/inventory`): filters category\|project\|approval\|location; full-text search across part + alt fields (200ms debounce); sort PN\|MFG P/N\|qty\|location\|min\|last-edited, persisted `localStorage` `viewPrefs:inventory` (whitelist-validated); scan panel shares the Vendor Search decoder (`VendorBarcodeScanPanel`, Enter/Tab terminator → instant decode, 250ms fallback debounce, camera modal, keystrokes re-render panel only); scan decode not visible in filtered view → server lookup `POST /api/inventory/search/barcode` — hit hidden by filters → clear-filters CTA, no hit → Vendor Search CTA; table row expands to alts; edit-all supports bulk location, direct qty set, consume, receive, min updates for primary+alt.
V48|Projects (`/projects`): status `active\|completed\|archived`; rows show nullable per-line alternative-class override + resolved class (§V59), lowest-break unit price + extended line total when distributor pricing exists; header shows rolled-up priced total + missing pricing. add by search or bulk-pasted MFG P/N. `Consume All` shows Class A/Unrated advisory but decrements specified rows without class gate/substitution. `Generate BOM` opens column picker (admin defaults) → CSV enriched with live component/distributor/alt data; `Alternative Class` available + selected in code default, explicit saved admin column choices preserved (§V17, `client/src/utils/bomExport.js`).
V49|Reports (`/reports`) live data + CSV export for: Library Quality, CAD Coverage By Category, Category Distribution, Estimated Inventory Value, Footprint Issues, Manufacturer Coverage, Low Stock. Audit (`/audit`): filter by search\|activity type\|date range\|page size, row expand shows JSON details, export filtered CSV, paginated.
V50|User Settings (`/user-settings`): account summary (username/role/sign-in method local\|SSO), profile (display name/email/file storage path), ECO notification prefs, delegated backup approver, password change local-only (SSO accounts see an IdP-managed note instead of the form).
V51|Admin Settings (`/admin-settings`) tabs: User (create/edit/activate/deactivate, retained-history soft removal, role change incl. `lab`, export/import user JSON); BOM (default export columns); Category (create/edit prefix+leading-zeros+name, global prefix rules, reorder, category specs, manufacturer rename/merge); ECO (number prefix+seq, approval stages/order/parallel groups/required roles/approvals/stage tags incl. `Shared Rename`/assigned approvers, export/import stages JSON, PDF branding, doc-control notify email); Email (SMTP host/port/auth/from/TLS, test connection/email, ECO template preview); Update (bulk stock/spec refresh oldest-sync-first per §V24, bulk distributor fill, library-folder scan for untracked CAD); Operation (verify schema, init defaults, export/import compressed DB backups, Filename Sanitization per §V64, destructive full reset/delete parts\|project data\|users); Logs (clear audit log after confirm).
V52|end-to-end operator paths: vendor-assisted new part = Vendor Search/scan → multi-select → send to Library → review category/numbering/specs/distributors/alts/CAD → save or later ECO. manual new part = Library add manual → category+required fields → specs/CAD/alts → save (inventory row created/repaired). controlled change = open saved part → start ECO → stage changes+notes → review/act in ECO → rejected → reload change-set + resubmit. shared file governance = link/upload in Library → File Library inspect reuse/rename/orphans/copy paths → admin library scan for unregistered disk files. stock→BOM = maintain Inventory qty → build Projects from saved parts → adjust/bulk import MFG P/N → consume → export BOM with chosen columns.
V53|global UX rules: guest login is first-class read-only; limited roles (`read-only\|reviewer`) never see Dashboard/File Library/Inventory/Projects/Reports/Audit nav; ECO visibility is feature-flagged; CAD uploads stage before save and stay reversible until save/cancel; footprint pairs stay grouped across upload/rename/File Library; add/edit conflict modal offers reuse-existing\|overwrite; ECO mode never allows live-file overwrite; ECO shared non-admin rename always warns before staging (admin renames direct); file-path copy depends on per-user storage base; BOM export columns default from admin but stays user-selectable per export; approval-stage assignees narrow action to named users; delegation allows backup action.
V55|tenant allowlist: `OIDC_ALLOWED_TENANTS` set -> callback ! require `tid` & reject absent\|unlisted value before DB write/cookie -> `/login?error=sso_failed`. exact Entra public-cloud tenant-independent issuer (`https://login.microsoftonline.com/common\|organizations[/v2.0]`) & allowlist unset -> SSO disabled w/ explicit config error. tenant-specific Entra issuer & generic OIDC issuer stay enabled when allowlist unset (§R5); URL checks ! parsed hostname/path, ⊥ substring.
V57|AD/OIDC supplies authentication identity; IdP `roles`\|`groups`\|`wids` & similar authorization claims ! ignored. OIDC JIT owns first local-row creation; app owns authorization via `users.role` + role gates/admin changes, and successful SSO ⊥ overwrite existing role/active state. optional Entra SCIM (§V60) may update linked identity profile + `users.is_active`, never role/password/OIDC keys. removal = deactivate + retain row, ⊥ `DELETE`, so historical authorship/approval/audit references survive; after local deactivation §V1 stops an existing JWT on its next protected request; Entra delivery cadence remains external.
V59|alternative criticality class domain = nullable `A\|B\|C`: A = substitute only with direct approval; B = substitute per drawing notes; C = substitute by component value; NULL displays `Unrated` + is operationally non-substitutable like A until rated. `components.alt_class` = library default; `project_components.alt_class` = per-BOM-line override; resolved = `COALESCE(project_components.alt_class, parent components.alt_class)` for direct or alternative line. new/existing rows may remain NULL. controlled component default changes obey §V15 and stage as normal ECO field under `spec` tag; project override is project data. class is advisory metadata this cycle: ⊥ automatic substitute choice or Consume-All block. DB CHECK + API boundary ! reject values ∉ domain. component-facing external views named §C4 append raw component default `alt_class` after existing columns.
V60|optional Entra SCIM lifecycle = single tenant, base `/api/scim/v2`, enabled iff valid GUID `SCIM_TENANT_ID` + ≥32-character `SCIM_BEARER_TOKEN`; partial/invalid config ! startup fail. tenant/object GUIDs canonical lowercase before TEXT compare. all endpoints incl. unsupported fallback ! constant-time, case-insensitive-scheme Bearer auth BEFORE any body parser; generic app JSON/urlencoded parsers ! skip SCIM subtree. responses + POST/PATCH bodies ! `application/scim+json`; malformed/oversize/wrong-media errors ! SCIM shape; token ⊥ log/docs. support User-only discovery incl. retrievable advertised individual ResourceType/Schema locations, `GET /Users?filter=externalId eq "<Entra objectId>"`, GET by local UUID, idempotent POST for already-linked (`oidc_tenant_id`,`oidc_object_id`) user → 201 + stable `Location`, case-insensitive PATCH Add/Replace + nullable-profile Remove, DELETE→inactive+204. Entra mapping ! immutable `objectId -> externalId`; local SCIM `id` = retained `users.id`. unknown identity ! empty query then POST 403 + safe audit/log, ⊥ create/link; operator flow = first OIDC login then retry provisioning. disable/unassign/delete → inactive; re-enable/reassign → same row active. SCIM may update `username\|display_name\|email\|is_active`; required `username\|active` ⊥ Remove. POST read-only `id\|meta` + empty `roles` ! ignore; nonempty `role\|roles` + any `password\|password_hash\|authProvider\|auth_provider\|oidc*` first-segment form ! refuse; PATCH `id\|meta\|externalId` ! mutability error & local auth/credential first-segment forms ! refuse. unsupported Group/Bulk/unknown filters or mutations return SCIM errors; replay/state repeats idempotent. after local inactive write, §V1 cuts off existing session on next protected request.
V61|canonical package identity = `<SHORT_NAME>[-<PIN_COUNT>]_<DENSITY>`, density ∈ `A\|B\|C` remapped from IPC-7351B material condition `M`→`A`, `N`→`B`, `L`→`C` (semantic, ⊥ alphabetic — §R18). legacy `_m\|_n\|_l` ! accepted on input @ ∀ boundary & emitted as the new letter, ⊥ rejected ∵ most vendor/online downloads still carry old letters. pin count ! trail the short name: vendor leading-count forms (DigiKey `8-SOIC`, `16-VQFN`, `10-VFDFN`) reorder to `SOIC-8`, `VQFN-16`, `VFDFN-10` @ ∀ sanitize boundary incl. 3D-model & symbol names. pin-count emission governed by per-package `count_policy ∈ {chip,embedded,none,append}` read from the catalog (§V62), ⊥ hard-coded heuristics: `chip` = 2-terminal size code is the whole identity (`0603`), `embedded` = count already in short name (`SOT-23-3`), `none` = count ∉ identity (`SOD-123`, `SMA`), `append` = `<SHORT>-<COUNT>` (`DIP-8`, `QFN-16`). full IPC-7351B dimensional names (`QFN50P500X500X80-29N`) are a recognized input form → collapse to family + pin qty + density.
V62|package catalog = DB-seeded truth: `packages` (canonical short name, family, mount, `count_policy`) + `package_aliases` (alias -> package, many-to-one per §R19/§R20). seed idempotent from `database/init-settings.sql` per §C7/§V4 & admin-extendable incl. `count_policy` per row; builtin rows (`is_builtin=true`) ! soft-disable on admin delete (`is_active=false`, row retained) ∵ §V4 re-seeds every boot & a hard DELETE would resurrect next boot — same deactivate-⊥-delete pattern as §V57; site-added rows (`is_builtin=false`) hard-delete; alias match case- & separator-insensitive (`TO-236-3` ≡ `TO236_3` ≡ `to 236 3`). parts-detail package field + footprint/model/symbol sanitizers ! resolve through this one catalog — a parallel client-only or code-constant package list ⊥. unknown package ⊥ blocks save: unresolved input passes through sanitized-but-uncanonicalized.
V63|CAD filename case policy is per file type. footprint (`.psm\|.bsm\|.dra`) ! whole-name lowercase on disk (§V28) ∴ on-disk density letter = `_a\|_b\|_c` & uppercase `_A\|_B\|_C` is display-only; upload/ZIP/finalize/rename/sanitize boundaries ! re-assert lowercase, ⊥ trust prior on-disk state. schematic-symbol & 3D-model keep user-input BASE case as-is, but extension ! lowercase both on disk & in UI display (`.STEP`->`.step`, `.OLB`->`.olb`) ∴ an uppercase CAD extension is ⊥ written & ⊥ displayed. MPN & package rename shortcuts emit UPPERCASE base for symbol & model, lowercase for footprint. `pad` & `pspice` ∉ package-canonicalization scope (§V61) & ∉ sanitization scope (§V64); their existing §V25/§V28 handling stays unchanged.
V64|Filename Sanitization = admin-only, explicitly-triggered maintenance action (Admin Settings -> Operation), ⊥ startup & ⊥ automatic ∴ `scanAndRegisterFiles` keeps register + missing-tag duties only & ⊥ renames. scope = `footprint\|symbol\|model`; `pad\|pspice` ∉ scope (§V63). transform = canonical identity per §V61 derived from the FILE's own name only — a name carrying ∄ package info is skipped + reported, ⊥ inferred from a linked component. gate ! name the shared-drive path + the §C4 OrCAD/CIS consequence (footprint rename regenerates `components.pcb_footprint`, which existing board designs read) + `BACK UP THE SHARED DRIVE FIRST` + type-to-confirm, before any run may start. ∀ rename through the atomic §V25 path (disk + `cad_files` + TEXT regen ∈ 1 txn); collision\|failure -> skip that file + log + continue, ⊥ abort the pass & ⊥ leave disk and DB divergent; footprint pairs rename together (§V53). ∀ rename & ∀ skip logged old->new + reason & returned in a run report. admin actor ∴ §V20 already permits direct shared-file rename w/ ⊥ ECO.
