# Codebase review coverage

This ledger tracks the multi-round review requested on 2026-09-07. A passing test gate is not proof that every module was reviewed. No live database or shared CAD files are modified by the review.

## Starting evidence

- Round 1: commit `a3f8e35` fixed project quantity/consumption validation, concurrent alternative inventory edits, and File Library nested controls. Coverage is reconstructed from that commit; no earlier root review ledger existed.
- Earlier repair cycle: `51cd758`, completed `PLAN.md`/`HANDOFF.md`, and `docs/reviews/2026-09-07-v1.11.0/review.md` cover backup restore and CAD selection/conflict repairs. These are historical evidence, not a fresh exhaustive review.
- Round 2 starts at `8388ee7` on `main`, with a clean worktree. Existing completed workflow documents remain historical; this review uses this file for continuity.

## Coverage map

| Area | Status | Examined paths and remaining scope |
|---|---|---|
| Inventory / projects | Round 1 partial | Quantity/consumption and alternative inventory writes fixed. Remaining controller reads, BOM/export, and UI flows still need review. |
| CAD component unlink / deletion | Round 2 reviewed/fixed | `fileUploadController.deleteFile`, `cadFileService.unlinkCadFileFromComponent`, `componentCadDelete.js`, `ComponentFiles` direct/add/ECO removal orchestration; shared variant dependencies, rollback and locked legacy endpoint policy checks. Other mutation endpoints' status-check races remain part of the next round. |
| CAD binding / variants | Round 2 partial review | `cadFileService` related-link lookup, auto-link, learning, ID/text synchronization; picker and related-selection helpers. Explicit association versus ambiguous co-usage established. Full multi-request binding-editor failure recovery and slot enforcement remain. |
| CAD File Library deletion / orphans | Round 2 reviewed/fixed | Service orphan query and single/group/bulk delete callers; active ECO references, whole-group rejection and concurrent component-link serialization. Filesystem failure/scan races remain below. |
| CAD upload / finalize / ZIP / restore | Round 4 ZIP ingress reviewed/fixed; shared lifecycle partial | `fileUploadController` upload/finalize/restore, `cadUploadService`, `cadArchiveService`, `Library` direct/add/ECO finalization and retry/cancel; per-file validation, locked edit policy, overwrite recovery, partial results, ZIP limits/CRC/size/cleanup and upload error UI. Process-crash/shared filesystem lifecycle remains. |
| CAD rename / sanitization | Round 3 legacy policy/single-file locking fixed; partial overall | `renameFile`, `renameCadFile`, staged pair rename and single-file rollback tests; all-consumer legacy policy and locked filename/consumer reads. File Library single/pair routing and sanitization callers traced; shared ECO staging/apply and cross-endpoint namespace concurrency remain. |
| Component CRUD / vendor intake | Partial trace; follow-up required | CAD create/update synchronization, upload identity selection, and existing deletion tests inspected; remaining fields, alternatives, distributor/spec writes, and UI save/cancel need review. |
| ECO | Partial trace; follow-up required | CAD resolve/stage/apply entrypoints mapped; full approval/retry/deletion/category-copy and shared-rename lifecycle need review. |
| Auth / OIDC / SCIM | Production review queued; SCIM test isolation fixed | Fresh review of middleware, controllers, services, config, session lifecycle, and role-matched UI. Round 4 inspected SCIM real-listener fixture, fixed port/mock isolation and ran 45 HTTP cases; this is not a fresh production-auth review. |
| Vendors / email / external services | Queued | Provider contracts, timeouts/retry, errors, data mapping, credentials, email/PDF generation. |
| Admin / database / migrations | Queued (backup repair historical) | Startup, schema/repair/reset, migration compatibility, package/catalog settings; revisit backup callers as needed. |
| Reports / audit / dashboard | Queued | SQL correctness, access boundaries, exports and UI states. |
| Shared UI / utilities / accessibility | Partial (Round 1 controls) | App routing/providers, common components, loading/error/empty states, keyboard accessibility, remaining utilities. |
| Build / deployment / import / dependencies | Queued | Root scripts, Docker/nginx, CI, manifests/lockfiles, legacy import; external support/advisory claims require current primary sources. |
| Assets / documentation / specification | Partial | Root guidance/spec and historical review read. Trace remaining documentation, templates, CAD assets as data, and configuration against consumers; binary CAD internals require specialist tooling. |

## Round 2 completed fixes

- R2-1 (high, fixed): deleting one footprint group collected every related pad/model even if another footprint on the same component still needed it. Server `deleteFile` and client `collectCadDeleteTargets` now exclude dependencies of retained footprints. Scratch PostgreSQL verifies `_a/_b/_c`, exclusive/shared/unbound pads, shared model, derived TEXT, unchanged disk records and relationship history. Rendered add/ECO tests remove three variants sequentially and retain dependencies until the last removal.
- R2-2 (high, fixed): `DELETE /api/files/delete` allowed write roles to bypass the controlled-component ECO edit policy. The handler now locks the component and checks current status before unlinking. Scratch tests reject all four controlled statuses without mutation and retain the admin exception.
- R2-3 (high, fixed): single/group File Library deletion could cascade away active ECO staging; bulk checks could race a new component link. All tracked-file deletion now locks the entire requested group, rechecks component and active ECO link/unlink/rename references, and rejects with 409 before any DB deletion. Disk unlink stays after commit. Orphan listing uses the same predicate and no longer hides unused files solely because an approved/rejected ECO once linked them. Scratch tests reproduce active-reference protection, whole-group rejection, successful unused-group removal, and a real FK lock wait followed by a committed concurrent link.
- R2-4 (medium, fixed): standalone `unlinkCadFileFromComponent` committed the junction deletion before TEXT regeneration. Both writes now share one transaction. A real CHECK-constraint failure verifies that the original link and TEXT survive rollback.
- R2-5 (high, fixed): `renameFile` used unvalidated `oldFilename` in its source path, allowing directory traversal outside the category. It now rejects non-leaf source names before filesystem access. Four formerly failing regressions cover POSIX/Windows relative and absolute paths; existing staged pair rename/rollback tests remain green.

## Remaining findings and focused follow-ups

- R2-6 (high, fixed in Round 3 for these legacy endpoints; original Round 2 evidence): legacy live `renameFile`, `finalizeTempFile` with `mfgPartNumber`, and `restoreDeletedFile` can mutate shared files/component links without the controlled-part check used by normal component APIs. `finalizeTempFile` accepts `resolution: 'overwrite'`, while the ECO overwrite prohibition is currently in `Library.jsx`. Next round must reproduce these requests and enforce server-side policy across all affected parts while preserving new-part/admin and unlinked ECO staging flows. This requires a coordinated review of upload, overwrite, shared rename and transaction ownership; the completed deletion fix does not close it.
- R2-7 (medium, fixed in Round 3 for finalization and Library batch handling; original Round 2 evidence): `moveToCategory` deletes the current target before attempting an overwrite move; failed move can lose the original. `autoLinkFileToComponent` swallows failures, and direct `Library.jsx` finalization discards temp state without checking per-file errors. Next round must inject move/register/link failures and make retry/cancel state reflect actual results.
- R2-8 (concurrency investigation; single-file rename reads and finalize/restore status checks fixed in Round 3, remaining route guards open): rename reads CAD rows/affected components before its transaction; standalone link/unlink route guards check component status outside their write transaction. Trace interleavings against ECO creation and component edits. No broad concurrency guarantee is claimed for these paths.
- R2-9 (recovery investigation): post-commit disk-delete failure leaves recoverable disk orphans under the existing contract. Re-registration/overwrite during that interval and the single-delete untracked-file fallback need coordinated filesystem/DB lifecycle review. Group DB deletion is atomic; multiple physical unlinks cannot be claimed atomic.
- Binding UX follow-up: multiple explicitly bound pads are represented as ambiguous single-choice candidates in the part picker; inspect whether the intended operator flow needs a selectable set. Global history alone does not distinguish alternatives from pads required together. No association semantics were invented in this round.

Round 2 deferred these follow-ups as a coherent upload/rename/ECO section. Round 3 dispositions and remaining work are recorded below; a green repository gate does not close the unexamined lifecycle paths.

## Binding behavior to preserve

`_a`, `_b`, and `_c` are separate footprint bases; each base groups its `.psm`/`.bsm`/`.dra` companions. Automatic learning requires exactly one footprint base and no more than one pad/model of each type. Multiple variants or multiple unbound pads are intentionally ambiguous: co-presence does not establish which pad belongs to which footprint. Explicit File Library bindings support multiple pads. Review must not fabricate these mappings or remove unrelated unbound files.

## Round 2 verification

- Before fixes: `componentCadDelete.test.js` failed the shared-dependency case; the 16-case scratch PostgreSQL removal suite failed 13 cases; four source-path regressions failed. These establish the observed defects, not merely expected query strings.
- After fixes: focused server removal/transaction slice passed 2 files / 25 tests; rendered selection suite passed 24 tests. Full gate below includes all other affected and inherited regressions.
- `bash ./test.sh`: PASS, exit 0; client/server/scripts lint fix and no-fix checks passed; client 38 files / 230 tests and server 65 files / 625 tests passed; scripts dry-run passed.
- Scripts category lookup still reports `connect EACCES 10.0.5.64:5435`; no live import/category mapping validated. Client jsdom logged an asynchronous network AggregateError while all assertions passed; no browser E2E or live UI walkthrough was performed.
- SQL behavior checked against PostgreSQL 18 [row-lock documentation](https://www.postgresql.org/docs/18/explicit-locking.html#LOCKING-ROWS) and [Read Committed semantics](https://www.postgresql.org/docs/18/transaction-iso.html#XACT-READ-COMMITTED), plus actual scratch FK/concurrency behavior. Disk mutations in regressions are mocked; scratch DB uses the repository's CAD table definitions and real cascade rules.
- Owned implementation/test diff reviewed for callers, shared dependencies, rollback, route authorization, error status propagation, and path boundaries. `git diff --check` passed. No schema or durable requirement changes; SPEC and completed cycle files remain unchanged. No dependency replacement, production write, push, or tag.
- Commit: the single summary commit containing this ledger, the fixes, tests, and Unreleased entries (locate with `git log -- REVIEW.md`).

## Round 3 coverage and fixes

Round 3 starts at `b8ef890` on `main` with a clean worktree. It continues the highest-impact unfinished CAD section; the queued non-CAD areas above are still unexamined. No delegation or live data writes.

- R3-1 (high, fixed; closes legacy R2-6 paths): `finalizeTempFile` and `restoreDeletedFile` now share `cadUploadService`. Target component ID or legacy MPN lookup locks current rows, rejects ambiguous/missing identities, and enforces the existing ECO edit policy before publication. Registration, junction insertion and TEXT regeneration share the transaction. New-part/ECO staging omits live identity; direct edits use the selected ID even if the MPN changes. Overwrite is rejected whenever ECO is enabled, including requests without a component and admin requests; this preserves the existing no-live-overwrite invariant. The conflict modal disables both overwrite controls.
- R3-2 (high, fixed; legacy rename portion of R2-6/R2-8): `renameFile` passes the authenticated actor into `renameCadFile`; the service locks the file and all current consumers before policy evaluation and mutation. A new-status supplied MPN cannot authorize changes to a shared file used by a controlled part. Admin direct rename remains allowed. Non-admin ECO-mode legacy renaming of untracked files requires scan first because consumer policy cannot be established from an absent CAD row. Staged temp renames stay available. File Library shared-ECO creation semantics are unchanged and require the remaining review below.
- R3-3 (medium, fixed; R2-7): finalization preserves an unresolved collision instead of discarding it. New targets use exclusive copying while retaining the temp token; overwrite backs up the old bytes and replaces without deleting the target first. Publication/link failure rolls back DB writes and restores disk state before releasing the transaction; incomplete physical recovery is reported explicitly. Temp cleanup occurs only after successful commit. Per-destination advisory locks serialize these finalization/restore requests, and registration holds the tracked row during publication.
- R3-4 (medium, fixed; R2-7 client side): direct/add/ECO Library save consumes per-token results. Partial success removes only successful tokens, stops the component/ECO save, and displays the failed filenames. Retry sends only remaining uploads; cancel only cleans those tokens. New-part finalization no longer risks linking by an MPN that belongs to another existing component. Each file is still a separate committed operation; this does not make an entire multi-file/component save atomic.

### Verification and evidence

- Nine scratch PostgreSQL regression cases fail against the pre-round controller/service, reproducing policy bypass, swallowed TEXT-write failure, ambiguous MPN linking, and shared legacy rename bypass. Fixed-version coverage uses the actual CAD schema/FKs, four controlled statuses, `lab/read-write/approver`, admin exceptions, unlinked staging, exact component identity, failed TEXT rollback, and a real concurrent status-update lock wait.
- Focused server slice: 3 files / 56 tests pass (`finalizeTempFile`, `cadDeletionPostgres`, `cadFileServiceTransactions`). Failure-injection filesystem model checks file bytes across move/register/link/commit failure, collisions, missing files, and retry. Scratch tests mock disk mutation; no real shared-drive writes are performed.
- Rendered client slice: 2 files / 5 tests pass (`libraryUploadRecovery`, `fileConflictModal`). Real Library orchestration is rendered with an isolated child form supplying staged tokens; retry/cancel/save API payloads and visible errors are asserted. Existing real-picker/ComponentFiles tests run in the full gate.
- Final `bash ./test.sh`: PASS, exit 0. Client/server/scripts lint fix and no-fix checks pass; client 39 files / 234 tests, server 65 files / 648 tests, scripts dry-run pass. An intermediate rerun caught `no-ex-assign` in the new recovery handler; corrected before this final pass. `git diff --check` passes.
- Commit: this ledger, reviewed fixes, tests, SPEC correction and Unreleased entries belong to the single `fix(cad): guard uploads and shared renames` commit (locate with `git log -- REVIEW.md`). No push or tag.
- External category lookup remains unavailable (`connect EACCES 10.0.5.64:5435`); scripts dry-run success does not validate a live import. No browser E2E, live SMTP/vendor/IdP call, production DB write, or deployment performed.
- Semantics checked against [PostgreSQL 18 row/advisory locks](https://www.postgresql.org/docs/18/explicit-locking.html) and [Node filesystem copy/rename documentation](https://nodejs.org/api/fs.html). Disk/DB coordination is compensating recovery, not a distributed transaction or process-crash guarantee; copy operations themselves are not atomic for external shared-drive readers.
- `SPEC.md` §V25 corrects the superseded move-then-register description; existing policy/atomicity obligations and IDs stay intact. Completed `PLAN.md`/`HANDOFF.md` describe the prior repair cycle and remain historical; no task is taken over or reset.

### Remaining findings and scope limits

1. ZIP uploads: closed by Round 4 limits/cleanup below. `footprintService` was checked and does not extract archives; raw HTTP download bounds remain in the external-services queue.
2. R2-8/R2-9 remain open across File Library group rename, shared ECO staging/apply, scan, untracked operations, and post-commit disk deletion. Standalone link/unlink status guards are now fixed in Round 4. No universal file namespace lock is established; cross-path deadlock/retry and post-delete re-registration interleavings require further fixtures.
3. Binding editor partial upload handling, multi-selection association semantics, and single-slot enforcement remain as previously listed. `FootprintLinkEditorModal` accepts successful results but does not yet give every failed upload a retry/cancel workflow. Repeated/concurrent whole-page saves and uploads arriving during an in-flight save also remain unverified.
4. ECO create/stage/approve/reject/retry/delete/category-copy/shared-rename lifecycle remains only partially traced. Auth/OIDC/SCIM, vendors/email/PDF, admin/startup/migrations, remaining inventory/projects, reports/audit/dashboard, common UI/accessibility, build/import/dependencies and assets/docs remain queued exactly as in the coverage map. No performance or dependency-safety claim is made from this CAD section; CAD binary internals require specialist tooling.

## Round 4 fixes and partial continuation

Starts at `ffd246060ac3d5f3db45c84587e1695078a58501` on `main`, clean worktree. Round 3 commit and files reconcile with the ledger; completed cycle documents remain historical. Examined this session: ZIP ingress/extraction, cleanup and consuming UI; standalone CAD link/unlink policy, transactions and callers; SCIM test fixture reliability. Shared rename/ECO inspection is partial. `footprintService` stages raw HTTP response bytes; it does not extract vendor ZIPs, so its download size/provider contract belongs to the queued external-services section.

ZIP and CAD final focused checks: 12 real-archive tests and 38 scratch PostgreSQL tests pass (50 total); rendered upload errors included in the full client gate (235 tests pass). A first fixture attempt revealed that CommonJS adm-zip bypasses the ESM fs mock: it wrote 11 uniquely timestamped test outputs to repository `library/temp`; all 11 exact names were removed immediately, and the fixture now injects the redirected filesystem into the real ZIP constructor. Later checks use scratch disk only; no pre-existing CAD data or production DB was changed.

The initial full gate found four native-fetch `bad port` failures in `scimRoutes.test.js`; unused one-shot mock results caused nine later incorrect-response assertions. This test defect is fixed below. A subsequent gate caught three duplicate-link insertions in the new test setup after successful API linking; fixture corrected and all 50 CAD cases rechecked. Final gate result recorded below.

Standalone link/unlink policy race reproduced on scratch PostgreSQL: both handlers returned success after an overlapping writer changed `new` to `reviewing`. They now lock/read/check the current component in the same transaction as junction and TEXT writes. Early middleware remains a fast rejection, not the final authority. SCIM transport repair passes all 45 real-listener cases; no SCIM production behavior changed. Shared rename/ECO and filesystem namespace coordination remain open, as detailed below.

### Findings and disposition

- R4-1 (high, fixed): a writable user's small ZIP could declare or expand to excessive content. Before the fix, bounded fixtures exceeded byte/count budgets successfully; installed adm-zip 0.5.18 also preallocated declared size. Admission now allows at most 1000 entries and 250 MiB total declared expansion per archive, and selected entries use independently bounded asynchronous inflation with exact size/CRC verification. Empty/stored and data-descriptor files remain supported; nested ZIP/7z entries are skipped. `adm-zip` upgraded to 0.6.0. No global request/concurrent-client resource quota is claimed.
- R4-2 (medium, fixed): later corrupt entries or failed disk writes orphaned earlier extracted files without cleanup tokens. Extraction now owns exclusive-created outputs, removes even partial writes on failure, and reports incomplete cleanup explicitly. Other files in the request retain their own results. Client ComponentFiles now displays filename/reason errors, including archive entry rejection, alongside successfully staged items. Source archive cleanup failure no longer discards unrelated batch results.
- R4-3 (high, fixed; standalone-link portion of R2-8): `/file-library/link` and `/unlink` could write a controlled component after an earlier middleware check allowed `new`. Locked policy checks now span junction and derived TEXT writes. Real status-writer interleavings failed before repair and pass afterward, preserving links/TEXT on denial; role/status matrix includes all four controlled statuses, new parts, admin, and ECO disabled.
- R4-4 (test reliability, fixed): SCIM listener tests used native fetch on arbitrary OS ports, causing `bad port` failures; `clearAllMocks` retained unconsumed queued mock values and cascaded false results. Local node:http transport retains real server status/media-type/body assertions and a bounded timeout; `resetAllMocks` isolates cases.

### Shared rename / ECO partial inspection; next fixtures required

- `fileLibraryController.renamePhysicalFile` and `renameFootprintGroup` collect file/consumer/status data before opening their staging transaction. `createMassFileRenameEco` trusts those snapshots when persisting `original_approval_status`; `restoreMassFileRenameStatuses` later writes that status unconditionally. A concurrent component status change between lookup and staging can therefore be restored to stale state. Code trace established; a real competing-writer fixture and full apply/reject/delete caller inspection remain prerequisites for a coordinated fix.
- Group physical rename preflights paths outside the transaction and does not lock file rows before moving bytes. `applyMassFileRenameEco` also reads current file names without row locks. Trace cross-path rename/finalize/delete interleavings and rollback ownership, including failures after apply returns but before the outer transaction commits. Existing within-helper rollback tests do not prove recovery from that later failure.
- Component-first linking/finalization and file-first single renaming can acquire locks in opposing orders. The link policy repair closes status bypass; it does not establish deadlock-free shared operations. Reproduce and select one coordinated lock order before claiming universal serialization.
- File Library UI/API explicitly stage shared renames only for more than one consumer, whereas legacy rename now checks every controlled consumer. Clarify single-consumer semantics against §V15/§V20 before changing this behavior; no requirement was removed to excuse the difference.

### Sources and scope limits

- Upstream [adm-zip 0.6.0 release](https://github.com/cthackers/adm-zip/releases/tag/v0.6.0) confirms CVE-2026-39244 and compatibility changes. Local package/lockfile and actual archive round trips inspected. [Node zlib](https://nodejs.org/api/zlib.html) supports bounded convenience-method output and CRC32 on the repository's Node 25 runtime. [Fetch port blocking](https://fetch.spec.whatwg.org/#port-blocking) explains the SCIM transport failure.
- No migration, production DB write, browser E2E, deployment, live vendor/SMTP/IdP call, push or tag. Scratch disk/PG and rendered jsdom checks cover the fixes. SPEC §V66 records the durable archive limits; prior completed PLAN/HANDOFF cycle remains historical and unchanged.
- Binary CAD internals and external performance measurements remain outside available tooling; no accessibility claim beyond inspected/rendered controls, and no whole-repository dependency audit. Queued areas in the coverage map remain open. Entire-codebase examination and shared lifecycle repair are not complete.

### Final verification for delivered fixes

- `bash ./test.sh`: PASS, exit 0. Client/server/scripts lint fix and no-fix checks pass; client 39 files / 235 tests, server 66 files / 669 tests, scripts dry-run pass. Final run includes the complete role/status matrix, archive fixtures, corrected fixture setup and lint-compliant assertions. `git diff --check` passes; full owned implementation/test/document diff and affected callers reviewed.
- Scripts category lookup still reports `connect EACCES 10.0.5.64:5435`; live category mapping/import is unverified. No live environment access was needed for the regression fixes. The archive fixtures use reduced injected budgets (1024 bytes / 4 entries), including an oversized declared-size header, rather than allocating adversarial gigabytes.
- Single summary commit: `fix(cad): bound archives and lock link policy` (locate via `git log -- REVIEW.md`). Includes this ledger, changelog, SPEC §V66, dependency update, fixes and tests. No push/tag. No unfinished implementation is included; remaining shared-rename findings require the next section's fixtures and coordinated design.

## Next continuation

Resume Round 4 shared rename/ECO inspection: build a scratch PostgreSQL fixture for `createMassFileRenameEco` using the real ECO tables, pause after File Library consumer lookup, commit a competing status change, then verify original-status staging and reject/delete restoration. Trace `applyMassFileRenameEco` through its outer approval transaction before editing rollback/locking. Continue R2-8/R2-9 rename/scan/deletion namespace coordination, full ECO lifecycle, then Auth/OIDC/SCIM and the remaining coverage map. The completed ZIP/link fixes do not close those sections.
