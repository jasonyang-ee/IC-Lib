1. Bug: The `link existing file` feature in edit and add mode of parts library did not auto link the pad of the footprint. It used to able to link the same pad that was learned and linked from other import. Please ensure this feature is working.
2. in file library, edit pad link, allow user to upload new file.
3. When uploading multiple footprints with multiple pads, the link are not working. User will need to have the ability to link the pads of the footprints during parts creation. Add this opertion UI feature.
4. Alt Class should not be shown in the parts library components view.
5. parts library components view should use font color on P/N only to indicate the Part status (production, review, proottype, archive).
6. The mass editing of "Set alternative class" in parts library page should be removed. We don't mass edit it.

