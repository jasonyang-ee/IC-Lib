# Vite Environment Variables

# API Base URL
# In development: use the backend server directly
# In production: leave empty or use '/api' (nginx will proxy)
VITE_API_URL=http://localhost:3500/api
