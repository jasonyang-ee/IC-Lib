Here are a list of fix and feature update that I want to do, please break down the tasks and plan for proper implementation, then implement the code.

1. On the `/library` file scan feature. I want it to be run on server start.
2. Also, the file scan feature will have to detect for both new files and deleted files. If someone delete file without going through our system, we should be able to detect that and mark the file as missing in the database. The parts that are missing files will be marked as "incomplete".
3. During new parts creation process, the rename feature did not work due to temp file handling. Please make sure user can rename the file on the fly during new parts creation. (see pasted image).
4. On edit mode, if user renamed file, please handle it with temp process. Only apply the name change after user click save, if user click cancel, we should also clean up the temp file and restore the deleted file if it is renamed.
5. Since the bug in point 4, I am now getting two symbol file record in the parts after an cancel of file rename. (pasted image 2). Please make sure the CIS filename list is generated from the CAD file management cad_file list. The current implementation has risk of mismatch between the two and cause bugs like this. We should have a single source of truth for the file management, which is the `cad_files` table and its relationship with components, and generate the CIS filename list based on that.
6. When symbol file has existing file, the popup window ask for "keep" vs "use new" option. The file extention sanitization is not applied. I am getting file with `.OLB` extension. Please make sure you apply the same file name sanitization for the new file name in this case as well.
7. Let's reorder the frontend display order of the CAD file types to be more intuitive: Symbol, Footprint, Pad, 3D Model, Pspice Model. Please also update the routeType mapping accordingly for the File Library navigation. See component details page for the current order. (pasted image 3).
8. File Library page, the file counts has bug, it is not couting the actual number of files, The center column has the correct counts. See pasted image 5. Please fix the file count logic to count the actual number of files for each category.
9. Let's update the file parsing logic for footprint file again. The `.dra` and `.psm` dual file exisistance is difficault to track. Currently, we are recording only `.psm` file in the parts library CAD file mamagement section, but the file library page will still find those `.dra` file and shown as unlinked. This is confusing for users. Let's update the logic to record the `.dra` file in the `cad_files` table and link it to the component. So both `.dra` and `.psm` files will be tracked in CAD File management section of parts library edit/add mode. Then, the CIS Filename list will only be generated using the `.psm` file for footprint.
10. I am getting a URI path of `http://localhost:5173/file-library/library` after edit mode. Please check the navigation logic after save/cancel in the edit mode, it should navigate back to the component details page instead of going to file library page. The correct is `http://localhost:5173/library` after save/cancel. Please fix the navigation logic accordingly.
11. Dashboard, please remove `Overall Completion` and make the Per-Type Completion to be row by row aligned to each Missing counts of each type. Currenty layout is messy.