Here are a list of fix and feature update that I want to do, please break down the tasks and plan for proper implementation, then implement the code.

1. schematic symbol can only accept one file, so if multiple files are uploaded in the parts library, show a popup windows for user to select "Keep Original" "Use New File" options. popup window should be reusable for other categories if needed in the future.
2. 3d step model can only accept one file, same behavior as schematic symbol.
3. On delete of CAD file, move it to temp folder instead of deleting immediately. Then, if user save the change from edit mode or add mode, delete the temp file. If user cancel the change, move it back to library folder. This way we can prevent accidental deletion of files that are still in use by other components.
4. When deleting file, our service must check if the file is linked to any component. If it is linked, we should not allow deletion. We should simply remove the link between the component and the file in the database, but keep the file in the library. This will prevent accidental deletion of files that are still in use by other components. Only if the file is not linked to any component, we can allow deletion from the library with following the temp folder process described in point 3.
5. When clicking the hyper link on compoenet details of the CAD file, it is currently jumping to the file library page but not filtering the file library to show the specific file. We can add query parameter to filter the file library page to show the specific file. For example, when clicking the hyper link of a footprint file, it will jump to the file library page and only show the footprint files with the same name. This will make it easier for users to find the files in the library and also see if there are any duplicate files.
6. In file library page, allow copy of file path as a text button on next to the file name. This will make it easier for users to copy the file path for use in other applications or for sharing with colleagues. Currently, file path is set using env variable, but depending on user local OS and NAS path setup, the base file path may change per user. So, still allow environment varible for global default to set `FILE_STORAGE_PATH`. Additionally, have this varible be set in User Settings page and be stored in the database per user. When copying file path, use the user specific base file path instead of the global default. This will allow users to have their own file storage setup and still be able to easily copy the correct file paths from the library.
7. In CAD file upload, we should sanitize the file extension name to make it all lower case. This will prevent issues with file management.
8. Let's start to add one new feature as admin management tool. I want the admin settings page, Auto Data Update section, to have a button to run for `/library` folder scanning to find if there is any file that is not in the cad_file table. If there is any file that is not in the cad_file table, add those files to database with the correct category based on the file extension and the file name. Then, those should be shown in the file library page with filtering "orphans". This will help us to keep track of all the files in the library and also make sure that all the files are properly linked to components. The scan should follow file extension rules for each category. For example, footprint files should have .dra and .psm file extension, symbol files should have .olb file extension, step model files should have .step or .stp file extension, pspice files should have .lib file extension, pad files should have .pad file extension. This will ensure that the files are categorized correctly when adding to the library. Ignore any files that do not match the file extension rules.
9. Please remove the init-sample-data.sql and any code that is using it. We should not have any sample data in the database. If we need to add sample data for testing, we can create a separate script that can be run manually to add sample data to the database. This will help us to keep the database clean and also prevent any accidental use of sample data in production.
10. In dashboard, Library Quality panel, the completion rate should be counted for each missing CAD file type. Please make the panel to be two columns. The second column will be completion rate for each CAD file type. The completion rate should not have colored background, simply use colored number and percent sign. Red if lower than 50%, yellow if between 50% and 80%, green if higher than 80%. This will make it easier for users to see the completion rate for each CAD file type and also identify which types of files are most commonly missing from the library. This will help us to prioritize our efforts in improving the library quality.
11. In parts library, component details panel, the multiple CAD files in each file type is too difficault to see the seperation of each file name. Please add a seperation line between each file name to make it easier for users to see the different files.
12. In parts library, Distributor Information panel, the SKU should not have underline as it is not a hyperlink.