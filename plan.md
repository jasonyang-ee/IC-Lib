1. We have worked on much of new feature that has not been released yet. There are some update required on the `/scripts/import.js` This import was made before we tried to manage actual CAD file in our system. This import script is very outdated. I think we need to make it work with our new schema. Then, we need to try to import the CAD file linking using the following logic:
  a. Use the string of `footprint` information to construct a list of file to search (those are a string of comma joint file name list), we need to find all files in the `/library/footprint` and do a case insensitive match for all `.dra` and .`psm` file. If any filename match regardless of the case. Please auto link those file with the corresponding part.
  b. Use the string of `schematic` information to construct file search. The string can contain `\` to indicate that the schematic is in `{Filename}\{Internal_Itemname}`. We only care about the Filename from import information. Then, using the filename do similar operation as step a to find all `.olb` or `.OLB` file in the `/library/schematic` folder. If any filename match regardless of the case. Please auto link those file with the corresponding part. Given an example `TPS54302DDCR\TPS54302DDCR` that is found in `/import/ICs_202510131650.csv` file. Then we will search for `TPS54302DDCR.olb` or `TPS54302DDCR.OLB` in the `/library/schematic` folder. If we find the file, we will link it to the part that is being imported.
  c. Use the string of `step_model` information to construct file search. The string will be the single CAD filename. We will search for that filename in the `/library/model` folder. If any filename match regardless of the case. Please auto link those file with the corresponding part.
  d. Use the string of `pspice` information to construct file search. The string will be the single CAD filename. We will search for that filename in the `/library/pspice` folder. If any filename match regardless of the case. Please auto link those file with the corresponding part.
  e. If no file is found for any of the above categories, we will leave the link empty for that category.
2. In File Library page, we need to add a "All Categories" view that will list all files in the library regardless of the category. This will help to find part number directly without going through category tree. (see pasted image)